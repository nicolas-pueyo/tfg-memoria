% !TeX encoding = UTF-8
\documentclass[12pt]{article}
\usepackage[spanish]{babel}
\usepackage[utf8]{inputenc}
\usepackage{graphicx} % Required for inserting images
\usepackage{geometry}
\usepackage{adjustbox}
\usepackage[most]{tcolorbox}
\usepackage[hidelinks]{hyperref}
\usepackage{parskip}
\usepackage{float}
\usepackage{blindtext}
\usepackage{csquotes}
\usepackage{svg}
\usepackage[
    backend=biber,
    style=numeric,
    sorting=none
]{biblatex}
\usepackage{microtype}
\usepackage{listings}
\usepackage{listingsutf8}
\lstdefinestyle{ascii-tree}{
	escapeinside={&*}{*&},
	literate=
	{├}{{\smash{\raisebox{-1ex}{\rule{1pt}{\baselineskip}}}\raisebox{0.5ex}{\rule{1ex}{1pt}}}}1 
	{─}{{\raisebox{0.5ex}{\rule{1.5ex}{1pt}}}}1 
	{└}{{\smash{\raisebox{-1ex}{\rule{1pt}{\baselineskip}}}}}1 % (O como lo tengas)
}
 \usepackage{dirtree}
 \usepackage{xspace}
 \usepackage{tabularx}
 \usepackage{array}
 \usepackage{booktabs}
 \newcolumntype{C}{>{\centering\arraybackslash}X}
 
\addbibresource{TFG.bib}

\title{Inteligencia Artificial On‑Premise para el análisis de IoCs con datos privados}
\author{Nicolás Pueyo Soria}
\date{Junio 2026}

\newcommand{\pipelines}{\textit{Pipelines}}
\newcommand{\OpenWebUI}{\textit{OpenWebUI}}
	
\setlength{\paperwidth}{21cm}          % Ancho de página
\setlength{\paperheight}{29,7cm}       % Alto de página
\setlength{\textwidth}{15.5cm}         % Ancho de zona con texto
\setlength{\textheight}{24.6cm}        % Ancho de zona con texto
\setlength{\topmargin}{-1.0cm}         % Margen superior
                                      
\setlength{\oddsidemargin}{0.46cm}     % Margen izquierdo 
\setlength{\evensidemargin}{0.46cm}    

\setcounter{secnumdepth}{4}
\setcounter{tocdepth}{4}
\newcommand{\subsubsubsection}[1]{\paragraph{#1}}

\makeatletter
\renewcommand\paragraph{\@startsection{paragraph}{4}{\z@}%
	{-3.25ex\@plus -1ex \@minus -.2ex}% Espaciado antes del título
	{1.5ex \@plus.2ex}% Espaciado después (un valor positivo fuerza el salto de línea)
	{\normalfont\normalsize\bfseries}} % Estilo de fuente (negrita, tamaño normal)
\makeatother



%%%%% DEFINICIÓN DE ESTILOS PARA LISTINGS
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
	backgroundcolor=\color{backcolour},   
	commentstyle=\color{codegreen},
	keywordstyle=\color{magenta},
	numberstyle=\tiny\color{codegray},
	stringstyle=\color{codepurple},
	basicstyle=\ttfamily\footnotesize,
	breakatwhitespace=false,         
	breaklines=true,                 
	captionpos=b,                    
	keepspaces=true,                 
	numbers=left,                    
	numbersep=5pt,                  
	showspaces=false,                
	showstringspaces=false,
	showtabs=false,                  
	tabsize=2
}

\lstset{style=mystyle}

\begin{document}

\pagenumbering{roman}
\renewcommand{\contentsname}{Índice}
\renewcommand{\labelitemi}{$\bullet$}
\renewcommand{\lstlistingname}{Listado}
\renewcommand{\lstlistlistingname}{Índice de listados}

\begin{titlepage}

\vspace*{-4mm}
\begin{figure}[!h]
  \centering
	\includegraphics[width=69.62mm]{Imagenes/UnizarLogo}
\end{figure}

\vspace*{17mm}

\fontsize{28pt}{28pt}\selectfont
\begin{center}
\setlength{\fboxsep}{3.4mm}
\adjustbox{minipage=14.4cm,cfbox=blue,center}{\begin{center} Trabajo Fin de Grado
\end{center}}
\end{center}

\vspace{18.7mm}


\fontsize{20pt}{20pt}\selectfont
\begin{center}
Inteligencia Artificial \textit{On-Premise} para el análisis de IoCs con datos privados
\end{center}

\vspace*{1cm} 
\baselineskip 36pt
\begin{center}
\fontsize{12pt}{12pt}\selectfont
\center{\rm  Autor}

\vspace*{3.65mm} 
\fontsize{18pt}{18pt}\selectfont
\center{Nicolás Pueyo Soria}
\vspace*{1.3cm}
\fontsize{12pt}{12pt}\selectfont
\center{\rm  Directores}
\vspace*{4.5mm}
\fontsize{14pt}{14pt}\selectfont
\center{Héctor Francia Molinero}
\center{Ricardo Julio Rodríguez Fernández}
\end{center}

\setcounter{footnote}{1}

\vspace*{16.45mm}
\fontsize{12pt}{12pt}\selectfont
\begin{center}
ESCUELA DE INGENIERÍA Y ARQUITECTURA\\
2026\\
\end{center}


\renewcommand{\thefootnote}{\arabic{footnote}}
\end{titlepage}


    
\newpage
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
 %%%%% Agradecimientos
 %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section*{Agradecimientos}

\setcounter{page}{1}

\newpage
\begin{abstract}
	\noindent El presente Trabajo Fin de Grado aborda el diseño, desarrollo e implementación de una plataforma privada y local (\textit{on-premise}) de asistencia conversacional adaptada a la ingeniería de ciberseguridad para el análisis seguro de Indicadores de Compromiso (IoCs). Ante el riesgo de fuga de información corporativa sensible mediante APIs comerciales, se propone una arquitectura modular de código abierto que garantiza la soberanía absoluta de los datos, operando bajo restricciones estrictas de hardware (procesamiento exclusivo por CPU y memoria RAM). La metodología de desarrollo implementada se basa en un sistema de Generación Aumentada por Recuperación (RAG) con un enfoque híbrido de extracción: por un lado, consume de forma automatizada grafos de conocimiento relacionales bajo el estándar STIX 2.1 desde la API de la plataforma de ciberinteligencia \textit{OpenCTI} y, por otro, recupera contexto analítico adyacente mediante búsquedas por similitud semántica en la base de datos vectorial ChromaDB. La interfaz interactiva del analista se gestiona mediante \OpenWebUI, orquestando los flujos a través de su servidor satélite \pipelines\ interconectado con un backend de servicios en \textit{FastAPI}.
	
	Los resultados cualitativos y cuantitativos, obtenidos mediante una rúbrica formal de evaluación sobre siete tipologías de consulta, demuestran la viabilidad del sistema y una alta estabilidad en los tiempos de inferencia local administrados por \textit{Ollama}. El análisis comparativo concluye que existe un balance crítico entre coste de cómputo y madurez cognitiva: mientras que el modelo ligero \textit{Lily-Cybersecurity-7B-v0.2} destaca por su velocidad pero muestra debilidades ante la ambigüedad conceptual, \textit{CyberOSS-2.0-20B} exhibe un razonamiento lógico superior para la resolución de grafos de atribución complejos. Finalmente, se establece que el rendimiento de los modelos pequeños se incrementa notablemente al guiar sus tareas mediante comandos concretos en lugar de peticiones generales, validando la utilidad operativa de la plataforma como asistente local en la respuesta automatizada ante incidentes.
	
	\vspace{1.5em}
	\noindent \textbf{Palabras clave:} Inteligencia Artificial, \textit{On-Premise}, RAG, \textit{OpenCTI}, \textit{ChromaDB}, \OpenWebUI, Indicadores de Compromiso (IoC), Ciberseguridad.
\end{abstract}

\newpage
\selectlanguage{english}
\begin{abstract}
	\noindent This final degree project presents the design, development, and implementation of a private, on-premise conversational assistance platform tailored for cybersecurity engineering to safely analyze Indicators of Compromise (IoCs). Addressing the risk of sensitive corporate data leakage associated with commercial cloud APIs, we propose a modular open-source architecture that guarantees absolute data sovereignty under strict hardware constraints, relying exclusively on CPU processing and RAM. The implemented methodology is based on a Retrieval-Augmented Generation (RAG) system utilizing a hybrid retrieval approach: it automatically ingests structured relational knowledge graphs under the STIX 2.1 standard from the \textit{OpenCTI} threat intelligence platform API, while concurrently retrieving adjacent analytical context through semantic similarity searches within a local \textit{ChromaDB} vector database. The analyst's interactive frontend is managed by \OpenWebUI, orchestrating data workflows through its satellite server \pipelines\ linked to a \textit{FastAPI} service backend.
	
	The qualitative and quantitative results, obtained through a formal evaluation rubric across seven query typologies, demonstrate the system's viability and high stability in local inference latencies managed by \textit{Ollama}. The comparative analysis concludes that a critical trade-off exists between computational cost and technical maturity: while the lightweight \textit{Lily-Cybersecurity-7B-v0.2} model excels in speed, it exhibits weaknesses when dealing with conceptual ambiguity; conversely, \textit{CyberOSS-2.0-20B} displays superior logical reasoning for resolving complex threat attribution graphs. Finally, the findings confirm that smaller-scale models significantly increase their precision when guided by concrete, task-specific commands rather than general queries, validating the platform's operational utility as a local assistant for automated incident response.
	
	\vspace{1.5em}
	\noindent \textbf{Keywords:} Artificial Intelligence, On-Premise, RAG, OpenCTI, ChromaDB, \OpenWebUI, Indicators of Compromise (IoC), Cybersecurity.
\end{abstract}
\selectlanguage{spanish}
\renewcommand{\tablename}{Tabla}
\renewcommand{\listtablename}{Índice de tablas}
\newpage
\tableofcontents

\newpage
\listoffigures
\listoftables
\lstlistoflistings

\newpage
\pagenumbering{arabic}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%% Introducción
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introducción}
\subsection{Contexto y motivación}
En los últimos años, la adopción de tecnologías basadas en Inteligencia Artificial (IA) se ha consolidado como una práctica habitual en el entorno corporativo para optimizar procesos y mejorar la productividad de los empleados. En particular, los Modelos de Lenguaje de Gran Escala (LLM) han demostrado ser herramientas de utilidad para tareas como la síntesis de información y la redacción de informes técnicos. No obstante, este rápido despliegue tecnológico también introduce retos de seguridad significativos. La falta de formación específica en ciberseguridad de muchos usuarios puede dar lugar a malas prácticas en el manejo de la IA, como la exposición accidental de datos corporativos sensibles al enviarlos a APIs comerciales externas.

De forma paralela, la defensa frente a incidentes digitales se apoya cada vez más en la Ciberinteligencia de Amenazas (CTI, por sus siglas en inglés), la cual permite estructurar y correlacionar datos sobre potenciales ataques mediante herramientas de código abierto como OpenCTI. Sin embargo, la gestión diaria de estos repositorios de conocimiento sigue requiriendo una considerable inversión de tiempo por parte de los analistas para procesar e interpretar la información técnica de los nuevos Indicadores de Compromiso (IoC).

En este escenario, surge la oportunidad de integrar la IA generativa como un asistente para facilitar la labor de los analistas en la interpretación y análisis de CTI. Para llevar a cabo esta integración en un contexto empresarial de manera segura, es necesario descartar las soluciones basadas en la nube y optar por modelos locales y privados. De este modo, este proyecto propone el desarrollo de una herramienta desplegada en un servidor privado que, mediante modelos de lenguaje especializados ejecutados localmente, permita a los ingenieros de ciberseguridad consultar, comparar y contrastar la ciberinteligencia de la organización con nuevos indicadores en un tiempo razonable, garantizando la soberanía de los datos sensibles de la empresa.

El presente proyecto se ha desarrollado en el marco de colaboración con la división de Zaragoza de Inetum España S.A., multinacional especializada en consultoría tecnológica y provisión de servicios de TI. En Aragón, Inetum desempeña un papel relevante al gestionar las infraestructuras y los servicios informáticos de entidades públicas de gran envergadura, como la Diputación General de Aragón (DGA) y el Servicio de Salud Aragonés (SALUD). En este contexto empresarial, la infraestructura asignada para el despliegue de la solución consta de entornos de virtualización Linux (RedHat 10), sin entorno de escritorio, sobre hardware de centro de datos basados únicamente en capacidad de procesamiento (CPU) y memoria RAM, prescindiendo por completo de aceleración gráfica por GPU. Además, con el fin de garantizar la soberanía, la transparencia y la viabilidad de la arquitectura propuesta, el sistema se implementará de manera íntegra utilizando herramientas y soluciones de software de código abierto.

\subsection{Objetivos del proyecto}

El objetivo general de este proyecto es diseñar, implementar y evaluar una plataforma privada y local de asistencia conversacional orientada a la ingeniería de ciberseguridad, integrada con una plataforma de gestión de ciberinteligencia de amenazas (CTI). El sistema propuesto debe permitir la consulta interactiva y el análisis contextualizado de Indicadores de Compromiso (IoC) garantizando la soberanía de los datos sensibles y optimizando el rendimiento de modelos de lenguaje bajo restricciones estrictas de hardware basadas exclusivamente en procesamiento por CPU y memoria RAM.

Para alcanzar este fin, se proponen los siguientes objetivos específicos:

\begin{itemize}
    \item \textbf{Despliegue y configuración de un entorno local aislado:} Implementar una infraestructura privada que albergue un entorno de ejecución para modelos de lenguaje y una interfaz gráfica de usuario accesible para los analistas, operando de manera local y sin dependencias de servicios externos en la nube.
    \item \textbf{Desarrollo de un pipeline de recuperación semántica:} Diseñar un mecanismo de indexación, almacenamiento vectorial y recuperación semántica de datos que permita alimentar las consultas del asistente conversacional con la ciberinteligencia y el contexto histórico de la organización de forma precisa.
    \item \textbf{Integración y consulta bidireccional con sistemas de ciberinteligencia:} Establecer una conexión con la API de la plataforma de ciberinteligencia del cliente para permitir que el asistente interactúe con el grafo de conocimiento existente, traduciendo consultas en lenguaje natural a búsquedas estructuradas de observables y amenazas.
    \item \textbf{Evaluación comparativa de modelos de lenguaje especializados:} Evaluar el rendimiento, el nivel de detalle técnico, la tasa de alucinaciones y la precisión de múltiples modelos de lenguaje de diferentes escalas de parámetros en tareas complejas de correlación y análisis de indicadores.
\end{itemize}
\subsection{Estructura del documento}
El presente documento se estructura en diferentes secciones con subsecciones. En la segunda sección se explican los conocimientos teóricos previos a tener en cuenta sobre diferentes conceptos clave para entender el desarrollo del proyecto. En la tercera sección se detalla cuál es la arquitectura del proyecto, qué componentes se han utilizado, qué tecnologías se han usado para desarrollar o desplegar cada uno de estos componentes, así como qué organización tienen y cómo se despliegan físicamente cada uno, y finalmente explica el workflow general del sistema, es decir, cómo cumple cada componente su cometido. En la cuarta sección se detallan las pruebas que se han realizado al sistema, se recogen y presentan los resultados obtenidos y se realiza un análisis de los mismos. Finalmente, en la última sección, la quinta, se analiza todo el contexto y trabajo desarrollado para extraer unas conclusiones, y se reflexiona sobre qué otras funcionalidades se podrían añadir al proyecto en caso de continuar con el desarrollo.
\newpage
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%% Conceptos previos
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conceptos previos}
\subsection{IR e IoCs}

En el ámbito de la ciberseguridad corporativa, la Respuesta a Incidentes (IR, por sus siglas en inglés de \textit{Incident Response}) constituye la metodología estructurada y sistemática que adopta una organización para la detección, contención, mitigación y recuperación frente a incidentes de seguridad y ciberataques. De acuerdo con los estándares de la industria, el objetivo primordial de la respuesta a incidentes abarca tanto la prevención proactiva de ciberataques antes de que estos lleguen a materializarse, como la minimización del impacto financiero, el daño reputacional y la interrupción de los procesos de negocio en caso de que una intrusión se complete de manera exitosa. 

Para operativizar esta estrategia, las organizaciones definen sus tecnologías, responsabilidades y procedimientos dentro de un Plan de Respuesta a Incidentes (IRP, \textit{Incident Response Plan}). Este plan actúa como el componente técnico de la gestión integral de incidentes, especificando cómo deben identificarse, aislarse y resolverse los diferentes vectores de ataque de forma coordinada. En la actualidad, la integración de soluciones de ciberseguridad asistidas por inteligencia artificial permite acelerar este proceso drásticamente, ayudando a los analistas a predecir posibles canales de ataque, automatizar la clasificación de alertas y correlacionar comportamientos sospechosos antes de que escalen a incidentes críticos.

Para el estudio del estado de la ciberinteligencia en el marco de la respuesta a incidentes, los analistas dependen de los Indicadores de Compromiso (IoCs, \textit{Indicators of Compromise}). En el campo de la informática forense y el análisis de malware, un IoC se define como cualquier artefacto, registro o evidencia digital identificado en un sistema operativo, un dispositivo de red o un flujo de tráfico que permite constatar, con un determinado nivel de confianza, la presencia de una actividad maliciosa o el compromiso de un activo. 

En el marco del estándar internacional STIX 2.1, la arquitectura de datos sobre la que se fundamenta la plataforma OpenCTI utilizada en este proyecto, es imperativo establecer una distinción técnica entre un \textit{Observable} (SCO, \textit{STIX Cyber Observable}) y un \textit{Indicador} (SDO, \textit{STIX Domain Object}):
\begin{itemize}
    \item \textbf{Observable (SCO):} Representa un dato técnico en bruto desprovisto de intencionalidad o valoración ética. Una dirección IP, un dominio o el hash de un binario son observables que pueden pertenecer tanto a servicios legítimos de la infraestructura como a servidores del atacante.
    \item \textbf{Indicador (SDO):} Es la entidad que aporta contexto semántico y de seguridad al observable. Un indicador asocia un patrón de búsqueda (definido bajo estándares como STIX Pattern, reglas YARA o firmas Sigma) a un observable específico, determinando un período de validez temporal, relaciones con campañas de actores de amenazas y un nivel de certeza técnica de maliciosidad.
\end{itemize}

Para el alcance del sistema desarrollado en este proyecto, se considerarán y analizarán los siguientes tipos de observables e indicadores de compromiso técnicos:
\begin{itemize}
    \item \textbf{URLs} identificadas como maliciosas, habitualmente vinculadas a servidores de descarga de cargas útiles (\textit{payloads}), portales de suplantación de identidad (\textit{phishing}) o paneles de control de botnets.
    \item \textbf{Hashes} de ficheros identificados como códigos dañinos. El uso de funciones de dispersión criptográfica permite identificar unívocamente muestras de malware sin necesidad de procesar el archivo completo. Para este proyecto se contemplan:
    \begin{itemize}
        \item \textbf{SHA256} (64 caracteres hexadecimales), estándar actual por su resistencia a colisiones.
        \item \textbf{SHA1} (40 caracteres hexadecimales), de uso remanente en bases de datos históricas.
        \item \textbf{MD5} (32 caracteres hexadecimales), obsoleto criptográficamente pero común en la categorización rápida de amenazas.
    \end{itemize}
    \item \textbf{Direcciones IPv4} identificadas como maliciosas, asociadas comúnmente a infraestructura de Comando y Control (C2), nodos de salida Tor o proxies utilizados para el escaneo de vulnerabilidades.
    \item \textbf{Dominios} web identificados como maliciosos, utilizados para enmascarar direcciones IP de infraestructura atacante mediante técnicas de resolución dinámica de nombres.
\end{itemize}

A nivel estratégico, la utilidad de estos IoCs se rige bajo la \textit{Pirámide del Dolor} de David Bianco\cite{davidjbianco2013}. Este modelo conceptual organiza los indicadores de amenazas según el grado de dificultad (o "dolor") que causa al atacante el hecho de que un defensor los detecte y bloquee:
\begin{enumerate}
    \item \textbf{Base (Hashes, IPs y Dominios):} Constituyen el nivel más bajo. Son extremadamente sencillos y baratos de modificar para el adversario. Un atacante puede alterar un solo byte de su código para cambiar el hash criptográfico del malware, o rotar de dirección IP en segundos mediante servicios de red dinámica, eludiendo bloqueos automatizados estáticos.
    \item \textbf{Medio (Artefactos de red y de host):} Requiere que el atacante modifique los patrones de comunicación de sus herramientas o las trazas que estas dejan en los registros de los sistemas comprometidos.
    \item \textbf{Cúspide (Herramientas y TTPs):} Las Tácticas, Técnicas y Procedimientos (TTPs) definen el comportamiento operativo real y los hábitos del atacante. Obligar a un atacante a cambiar sus TTPs es el logro de mayor valor defensivo, ya que le exige reestructurar por completo su metodología y reaprender tácticas de infiltración.
\end{enumerate}

La integración de la Inteligencia Artificial propuesta en este trabajo encuentra su motivación en esta jerarquía. Mientras que los sistemas automáticos de seguridad tradicionales (como cortafuegos o antivirus) son excelentes procesando de manera rígida los observables de la base (hashes e IPs), carecen de la capacidad cognitiva para relacionar estos datos con comportamientos complejos. La incorporación de un modelo de lenguaje de gran escala (LLM) permite a los ingenieros de ciberseguridad procesar el texto no estructurado y los grafos de conocimiento de OpenCTI para correlacionar observables simples con el comportamiento general de la amenaza en la cúspide de la pirámide, forzando un impacto real sobre el coste operativo del atacante.

\subsection{Ciberinteligencia de Amenazas (CTI) y OpenCTI}
La Ciberinteligencia de Amenazas (CTI, por sus siglas en inglés de \textit{Cyber Threat Intelligence}) consiste en la recopilación, análisis y estructuración de información sobre amenazas activas o potenciales con el fin de anticipar, detectar y mitigar ataques de forma proactiva. Esta disciplina permite a los equipos de seguridad comprender el contexto, las motivaciones y las metodologías de los adversarios, optimizando la toma de decisiones estratégicas y los tiempos de respuesta ante incidentes.

Para centralizar y procesar este conocimiento, las organizaciones emplean plataformas especializadas (TIP, \textit{Threat Intelligence Platforms}) como OpenCTI, una herramienta de código abierto diseñada para estructurar, almacenar, organizar y visualizar información técnica y no técnica sobre ciberamenazas. El núcleo de OpenCTI se fundamenta en un grafo de conocimiento basado en el estándar internacional STIX 2.1. En esta arquitectura, la inteligencia se modela mediante dos elementos\cite{openctidata}:
\begin{itemize}
    \item \textbf{Nodos (Entidades):} Representan conceptos de alto nivel, conocidos como objetos de dominio o \textit{STIX Domain Objects} (SDO), tales como Malware, Actores de Amenazas o Vulnerabilidades.
    \item \textbf{Aristas (Relaciones):} Modelan las conexiones lógicas entre las entidades y se asocian a observables técnicos o \textit{STIX Cyber Observables} (SCO), como direcciones IP, dominios o hashes de archivos, que sirven de evidencia de la actividad.
\end{itemize}

\subsection{Modelos de Lenguaje de Gran Escala (LLM)}
Los Modelos de Lenguaje de Gran Escala (LLM, por sus siglas en inglés) son sistemas basados en redes neuronales profundas diseñados para comprender, procesar y generar lenguaje natural. Formalmente, un LLM se define como una función matemática compleja cuya entrada y salida consisten en listas de números enteros\cite{llm2026}. Para que el modelo pueda procesar el texto en lenguaje humano, este debe someterse a un proceso de tokenización, en el cual se divide el flujo de caracteres en unidades discretas menores llamadas tokens (que pueden ser palabras, subpalabras o caracteres individuales)\cite{stryker2021}. Este proceso mapea el texto a valores enteros dentro de un rango determinado por el tamaño de su vocabulario $[0, V-1]$, donde $V$ representa la dimensión del vocabulario del modelo\cite{llm2026}.

En el contexto corporativo, la adopción de estos modelos se divide principalmente entre servicios de infraestructura gestionada en la nube y despliegues locales. Si bien el uso de APIs comerciales en plataformas en la nube (como las soluciones de infraestructura de AWS) proporciona una gran capacidad de cómputo y escalabilidad, la transferencia de datos de seguridad hacia servidores externos introduce riesgos severos de exposición, fugas de información y fallas de cumplimiento normativo. Por esta razón, el estado del arte en ciberseguridad se orienta firmemente hacia el despliegue local de modelos de pesos abiertos de menor tamaño, garantizando la soberanía absoluta de los datos sensibles de la organización\cite{amazon2026llm}.

\subsection{Motores de inferencia local}

Mientras que un LLM representa la estructura estática de la red neuronal y sus pesos matemáticos almacenados en disco, el motor de inferencia es el software de ejecución (\textit{runtime}) activo que carga dichos pesos en memoria y realiza las operaciones algebraicas necesarias para responder a las consultas del usuario. En un servidor privado que carece de aceleradores gráficos (GPU), el motor de inferencia debe ejecutar el modelo utilizando exclusivamente el procesador (vCPU en contextos de centros de datos) y la memoria RAM del sistema.

En este escenario de inferencia puramente basado en CPU, la velocidad del sistema se ve fuertemente limitada. El cuello de botella principal no radica en la capacidad de cómputo de la vCPU, sino en el ancho de banda físico de la memoria RAM, puesto que el procesador debe leer la totalidad de los parámetros del modelo para generar cada token individual. Mientras que las GPUs utilizan memoria especializada de gran velocidad con un ancho de banda de entre 600 y 1500 GB/s, la memoria RAM convencional de un servidor host suele transferir datos a velocidades de entre 50 y 200 GB/s. 

Esta limitación física provoca que la asignación de hilos de ejecución en el motor de inferencia no escale de manera lineal con el número de núcleos virtuales (vCPUs) asignados. Si se configuran demasiados hilos de ejecución concurrentes en el motor de inferencia para procesar un modelo pesado, los hilos terminarán compitiendo por los mismos canales físicos de acceso a la memoria RAM. Esta contención de memoria genera un cuello de botella técnico que degrada drásticamente el rendimiento del sistema y reduce la tasa de generación de texto (tokens por segundo), por lo que la optimización de estos sistemas requiere limitar de forma explícita el número de hilos de ejecución a un valor cercano al número de núcleos físicos del procesador.

\subsection{Generación Aumentada por Recuperación (RAG)}
La Generación Aumentada por Recuperación (RAG, por sus siglas en inglés de \textit{Retrieval-Augmented Generation}) es una técnica que optimiza el comportamiento de un modelo de lenguaje al complementar sus instrucciones (\textit{prompts}) de entrada con información externa relevante sin necesidad de modificar físicamente los parámetros del modelo\cite{stryker2021}. El flujo operativo de RAG consta de tres etapas lógicas:
\begin{enumerate}
    \item \textbf{Recuperación:} Ante una consulta del analista, el sistema busca de manera automática los fragmentos de documentos o informes técnicos más relevantes dentro de una base de datos.
    \item \textbf{Aumentación:} El sistema añade estos fragmentos recuperados al \textit{prompt} original del usuario en forma de contexto informativo verídico.
    \item \textbf{Generación:} El LLM procesa la consulta enriquecida y genera una respuesta redactada en lenguaje natural que se fundamenta estrictamente en la evidencia proporcionada, reduciendo de manera drástica las alucinaciones del sistema\cite{stryker2021}.
\end{enumerate}

Para dar soporte y agilizar la etapa de recuperación en tiempo real, se emplean las bases de datos vectoriales (como ChromaDB o Qdrant), las cuales almacenan los textos de ciberinteligencia previamente convertidos en vectores numéricos o \textit{embeddings} que capturan su significado semántico. De este modo, la base de datos es capaz de localizar de forma matemática los fragmentos de información más pertinentes según la intención de la consulta del analista, inyectándolos instantáneamente en el contexto de la IA\cite{llm2026}.

\newpage
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%% Análisis, diseño e impleçmentación del sistema
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Análisis, diseño e implementación del sistema}
Tomando como punto de partida la infraestructura tecnológica preexistente de la organización, la cual dispone de una instancia de OpenCTI desplegada de manera independiente en un nodo dedicado de la red corporativa, el desarrollo práctico de este trabajo se centra en diseñar e implementar una pasarela de comunicación bidireccional y segura con dicha base de conocimiento.

Con el propósito de estructurar de forma modular el asistente inteligente, cuya finalidad es procesar y correlacionar información de seguridad  dentro del perímetro de la empresa, la arquitectura del sistema propuesto se articula en torno a los siguientes componentes:
\begin{itemize}
    \item \textbf{Motor de inferencia local y selección de modelos:} Para la ejecución de los modelos de lenguaje de gran escala bajo un entorno local, se evaluaron diversas alternativas de motores de inferencia, entre las que destacaron Ollama y vLLM. Si bien vLLM ofrece un rendimiento excepcional en la gestión de solicitudes concurrentes gracias al algoritmo de optimización de memoria \textit{PagedAttention} \cite{kwon2023paged}, su arquitectura está diseñada de manera prioritaria para operar bajo aceleración por hardware gráfico (GPU). Debido a la restricción de infraestructura impuesta en este proyecto, consistente en operar exclusivamente sobre procesamiento por CPU, se seleccionó Ollama como el motor de inferencia local principal. Ollama, desarrollado sobre la biblioteca nativa en C/C++ llama.cpp, proporciona una capa de abstracción eficiente para entornos virtualizados, facilidad de configuración multimodelo mediante archivos de instrucción (\textit{Modelfiles}) y una API local compatible con los estándares de la industria.

    Para posibilitar la interacción con el motor, los modelos de lenguaje se cargan en su versión cuantizada utilizando archivos de extensión \texttt{.gguf}. En cuanto a los modelos de lenguaje seleccionados, se descartaron las opciones generalistas en favor de modelos especializados en ciberseguridad mediante \textit{fine-tuning}. Esta especialización garantiza que la IA sea capaz de interpretar terminología técnica de ciberinteligencia, correlacionar observables y procesar de manera segura datos potencialmente ofensivos (como volcados de registros o firmas de malware) sin sufrir de bloqueos por políticas de seguridad corporativas comerciales. Con el fin de realizar una evaluación comparativa bajo los recursos asignados al servidor, se seleccionaron dos modelos especializados de escalas de parámetros diferenciadas:
    \begin{itemize}
        \item \textbf{Lily-Cybersecurity-7B-v0.2:} Modelo ligero desarrollado por Sego Lily Labs, ajustado sobre la arquitectura de Mistral-7B-Instruct-v0.2 a partir de un conjunto de 22,000 instrucciones de seguridad manuales \cite{segolily2024lily}. En su cuantización a 4 bits, presenta un peso de archivo de aproximadamente 4.37 GB, lo que permite una inferencia ágil y fluida con un consumo mínimo de memoria RAM.
        \item \textbf{CyberOSS-2.0-20B (CyberPal-2.0-20B):} Modelo avanzado de la suite de seguridad de código abierto CyberPal 2.0, ajustado sobre la arquitectura gpt-oss-20b mediante el pipeline de enriquecimiento y trazado de razonamiento técnico \textit{SecKnowledge 2.0} \cite{levi2025toward}. En su cuantización de 4 bits (Q4\_K\_M), el tamaño del archivo asciende a 15.8 GB, demandando un mayor esfuerzo de lectura por parte de la memoria RAM del servidor, pero aportando un análisis conceptual de nivel experto.
    \end{itemize}

    La elección de esta pareja de modelos permite someter la herramienta a una validación práctica frente a los benchmarks de referencia de la literatura científica. De acuerdo con el estudio de evaluación integral publicado en "Toward Cybersecurity-Expert Small Language Models" (arXiv:2510.14113) \cite{levi2025toward}, Lily-Cybersecurity-7B-v0.2 obtiene una puntuación promedio de 53.38\% en la resolución de tareas complejas de seguridad y ciberinteligencia, mientras que CyberPal-2.0-20B alcanza una puntuación de 80.33\% en el mismo conjunto de pruebas (CTI-Bench), superando incluso a modelos comerciales cerrados en la correlación de vulnerabilidades e identificación de tácticas.
    \item \textbf{Interfaz gráfica de usuario y orquestación de flujos (\OpenWebUI{}):} Para proporcionar a los analistas de ciberseguridad un entorno visual, accesible e interactivo, se evaluaron diversas plataformas autohospedadas capaces de desplegarse mediante contenedores Docker y configurarse de manera sencilla a través de parámetros de entorno. En una primera fase de diseño, se contempló la implementación de la interfaz \textit{ChatUI} desarrollada por Hugging Face. Sin embargo, este entorno fue descartado debido a incompatibilidades severas en su integración nativa con el motor de inferencia local Ollama. Durante las pruebas de concepto, la ambigüedad en la documentación oficial respecto a su interconexión local propició fallos críticos en el control del estado, tales como la imposibilidad de mapear y listar dinámicamente los modelos de Ollama, y respuestas de los modelos no mostradas en la interfaz.

    Para solucionar estos inconvenientes, se seleccionó la plataforma \OpenWebUI{}. Esta herramienta destaca por su compatibilidad nativa e inmediata con la API de Ollama, configuración de parámetros tanto de forma declarativa (en la configuración de Docker) como en caliente desde su panel de administración web \cite{openwebui}. Un factor tecnológico determinante para su elección fue la disponibilidad de su arquitectura desacoplada de flujos de trabajo, denominada \pipelines. Este servidor satélite programado en Python actúa como un \textit{middleware} capaz de interceptar, filtrar y modificar las peticiones dirigidas al modelo de lenguaje \cite{pipelines}. Esta capacidad resulta crítica para el alcance de este proyecto, ya que proporciona el punto de anclaje necesario para inyectar lógica personalizada, realizar llamadas a servicios externos y canalizar las búsquedas de ciberinteligencia dentro de la base de datos vectorial de forma transparente para el analista, facilitando así la integración del sistema RAG.
    
    \item \textbf{Backend, motor de RAG y pipelines:} Este bloque representa el núcleo diferencial y el valor de desarrollo propio de este proyecto. Mientras que los componentes analizados hasta ahora se basan en el despliegue, parametrización y organización arquitectónica de imágenes de Docker (como \OpenWebUI{} o la base de datos vectorial), es en esta capa donde se introduce la lógica personalizada que gobierna el flujo, tratamiento y enriquecimiento de los datos de seguridad. Aunque se describen como componentes diferenciados, el servidor de \pipelines, el backend de servicios y el motor de RAG operan como un único subsistema lógicamente acoplado debido a su constante cooperación en tiempo real. 

    El flujo de procesamiento de la información se inicia cuando el analista realiza una consulta en la interfaz gráfica que menciona un indicador de compromiso (IoC), momento en el cual la solicitud es interceptada de manera inmediata por el servidor de \textit{middleware Pipelines}. En lugar de transmitir la petición de forma directa al motor de inferencia local, el \textit{pipeline} extrae el \textit{payload} y desvía la solicitud hacia un \textit{endpoint} específico de nuestro backend. Al recibir este paquete, el backend analiza la petición y llama a las funciones del motor de RAG, el cual recupera contexto técnico sobre el indicador consultando de manera estructurada tanto la API de OpenCTI como la base de datos vectorial local (ChromaDB). Una vez obtenido este contexto histórico y relacional, se devuelve en forma de respuesta al \textit{pipeline}, encargado de reformular el \textit{prompt} del usuario inyectando la información recuperada y de transmitir la instrucción final enriquecida al modelo correspondiente para que este realice su razonamiento.

    Para la construcción del backend de servicios se seleccionó el marco de trabajo (\textit{framework}) FastAPI. Esta elección se fundamenta principalmente en su sencillez y en que permite un desarrollo de \textit{endpoints} de manera rápida y eficiente, facilitando un acoplamiento ágil con el resto de componentes escritos en Python. Asimismo, FastAPI destaca por su capacidad para gestionar de manera óptima la asincronía de forma nativa a través del servidor web de aplicaciones de bajo nivel Uvicorn, encargado de procesar la especificación ASGI de manera eficiente. Como un añadido a esta infraestructura, FastAPI utiliza internamente Starlette para dar soporte a la asincronía y se integra de forma directa con Pydantic para la validación declarativa de datos mediante anotaciones de tipo, lo que garantiza la integridad de los \textit{payloads} intercambiados sin penalizar la velocidad de desarrollo.
    
    \item \textbf{Motor de base de datos vectorial (ChromaDB):} La incorporación de este componente responde a la necesidad de complementar la información relacional y estructurada obtenida de la API de OpenCTI con capacidades de recuperación semántica profunda. Mediante el uso de un modelo local de generación de vectores (\textit{embeddings}), el sistema procesa, fragmenta e indexa tanto los informes técnicos de amenazas como los metadatos de los observables extraídos de la plataforma de ciberinteligencia. De este modo, ante una consulta del analista, la base de datos permite ejecutar búsquedas de similitud para deducir inferencias latentes y proporcionar un contexto complementario de alto valor que no se encuentra explícitamente enlazado en el grafo de conocimiento original.

    Para la implementación de este almacén de vectores se seleccionó \textit{ChromaDB}. En primer lugar, se define como una base de datos vectorial de código abierto altamente ligera y optimizada para despliegues locales (\textit{on-premise}), lo que evita introducir una sobrecarga de infraestructura compleja o costosa en el servidor. En segundo lugar, su arquitectura modular permite que la base de datos se ejecute de forma embebida directamente dentro del proceso de Python o de manera independiente mediante un contenedor, lo que optimiza drásticamente el uso de recursos y memoria RAM en entornos limitados a CPU. Finalmente, destaca su excelente compatibilidad con el ecosistema de desarrollo de Python y su amplia adopción en la comunidad, lo que garantiza una API sencilla para operaciones de inserción, consulta, actualización y borrado (CRUD), así como un soporte nativo para el filtrado avanzado de metadatos.
    
    \item \textbf{Módulo de comunicación, ingesta y normalización de ciberinteligencia:} Este componente representa la capa de software desarrollada a medida para interactuar con la plataforma OpenCTI, resolviendo la adquisición de datos de seguridad y su transformación hacia la base de datos vectorial. La conectividad lógica con el servidor se implementa bajo el patrón de diseño \textit{Singleton} con soporte multihilo (\textit{thread-safe}) mediante un mecanismo de exclusión mutua (\textit{lock}), garantizando que exista una única instancia activa del cliente de API oficial (\texttt{OpenCTIApiClient}) durante la ejecuPartes del sistema ...ción del sistema, lo que evita la saturación de conexiones GraphQL en el nodo de OpenCTI.

Sobre esta infraestructura de conexión se articulan tres subcomponentes de desarrollo propio en Python:
\begin{itemize}
    \item \textbf{Cliente de consultas semánticas (\texttt{OpenCTIRAGClient}):} Componente que se ejecuta en tiempo real dentro del flujo de la conversación. Permite realizar búsquedas exactas e intermedias sobre observables e indicadores bajo la estructura del estándar STIX 2.1, rastreando de forma automática las relaciones lógicas (\textit{relationships}) creadas por los analistas y localizando reportes técnicos relevantes que sirvan como evidencia contextualizada para el modelo.
    \item \textbf{Ingestor de entidades (\texttt{OpenCTIIngestor}):} Módulo encargado del aprovisionamiento masivo de datos históricos. Implementa el protocolo de paginación oficial basado en cursores de OpenCTI (\texttt{withPagination=True} y \texttt{endCursor}) para descargar de forma secuencial y ordenada colecciones completas de actores de amenazas, malware, tácticas, reportes e indicadores técnicos, almacenando la información de ciberinteligencia en bruto localmente en formato JSON.
    \item \textbf{Normalizador semántico e indexador (\texttt{OpenCTINormalizer}):} Clase de vital importancia para el RAG que resuelve el grafo de relaciones de OpenCTI (como la arista \textit{uses} que vincula a un actor de amenazas con un malware específico o un TTP de MITRE ATT\&CK). El normalizador extrae las relaciones complejas y las unifica en plantillas textuales estructuradas y metadatos legibles para la IA. Posteriormente, un script de vectorización automatizado codifica estos textos normalizados en representaciones densas de 384 dimensiones empleando el modelo local \texttt{all-MiniLM-L6-v2}, insertándolos finalmente mediante un algoritmo por lotes (\textit{batches}) en ChromaDB bajo una métrica de similitud de distancia del coseno para dar soporte a las búsquedas de proximidad semántica.
\end{itemize}
\end{itemize}
\subsection{Arquitectura del sistema}
La arquitectura de la solución propuesta se ha diseñado siguiendo un paradigma modular y desacoplado. Por restricciones corporativas, sólo se dispone de una máquina virtual, por lo que todo está desplegado en la misma. No obstante, el sistema está diseñado para ser fácilmente distribuible. Esta topología, detallada en el diagrama de componentes de la Figura~\ref{fig:ArquitecturaCyC}, permite separar de forma estricta el plano de interacción con el usuario e inferencia local del plano de ingesta, normalización y ciberinteligencia de amenazas.

A nivel de infraestructura, la comunicación entre los componentes se realiza a través de puertos y endpoints específicos dentro de una red virtual interna, articulándose en dos bloques tecnológicos principales:

\begin{itemize}
    \item \textbf{Dominio de Interacción e Inferencia Local (Plano Izquierdo):} 
    Este dominio gestiona la interfaz del analista y la ejecución de la IA. El componente \OpenWebUI actúa como el frontend del sistema, comunicándose directamente con el puerto \texttt{9099} del \textit{Pipeline Server}. Este último actúa como un \textit{middleware} de intercepción y enrutamiento, derivando las consultas de enriquecimiento hacia el \textit{Backend RAG} en el puerto \texttt{9000}, concretamente al endpoint \texttt{/query-context} y transmitiendo el prompt final aumentado hacia el endpoint \texttt{/api/chat} de \textit{Ollama} en el puerto \texttt{11434}, el cual expone localmente los modelos \textit{Lily} y \textit{CyberOSS}.
    \item \textbf{Dominio de Ciberinteligencia, Ingesta y Vectorización (Plano Derecho):} 
    Este dominio administra la adquisición de conocimiento de seguridad. El componente \textit{OpenCTI Client} encapsula la conexión única hacia el puerto \texttt{8080} de la instancia externa de \textit{OpenCTI}. De este cliente dependen de manera exclusiva el \textit{Ingestor} (responsable del aprovisionamiento masivo de SDOs y SCOs) y el \textit{Backend RAG} (responsable de las consultas semánticas y relacionales en caliente). Ambos módulos escriben y consultan, respectivamente, la base de datos vectorial \textit{ChromaDB} a través de su API REST en el puerto \texttt{8080}.
\end{itemize}
\begin{figure}[h!]
    \centering
    \includesvg[width=0.8\linewidth, scale=1.5]{Imagenes/Arquitectura_CyC.svg}
    \caption{Diagrama de componentes}
    \label{fig:ArquitecturaCyC}
\end{figure}
\begin{figure}
    \centering
    \includesvg[width=1\linewidth]{Imagenes/Diagrama_de_despliegue}
    \caption{Diagrama de despliegue con comunicaciones}
    \label{fig:despliegue}
\end{figure}

\subsection{\textit{Workflow} del sistema}
   En la Figura~\ref{fig:despliegue} se detalla concretamente la organización del despliegue de todos los artefactos mencionados, así como las comunicaciones que tienen entre ellos. Las comunicaciones se expresan como flechas etiquetadas. Una flecha bidireccional ($\longleftrightarrow$) significa que se manda una \textit{request} y se espera \textit{response} (por ejemplo, la flecha etiquetada con $1$ entre \OpenWebUI y \pipelines, mientras que una flecha en una sola dirección ($\longrightarrow$) significa que simplemente se envían datos en una sola dirección (esto solo se da entre el \textit{ingestor} y \textit{ChromaDB}, etiquetada con $II$).

    Una vez definida la distribución lógica y física de la infraestrutura, es fundamental definir la dinámica temporal que sigue el sistema. Como se ha mencionado previamente, el sistema sigue dos flujos de datos diferentes, por una parte está la ingesta, vectorización y almacenamiento de datos de OpenCTI (módulo \textit{Ingestor} que interactúa con \textit{OpenCTI} y \textit{ChromaDB}), cuyas comunicaciones están etiquetadas con números romanos en el la Figura~\ref{fig:despliegue} ($I$ y $II$).
    
  Finalmente, en la Figura~\ref{fig:tree} se detalla dentro del propio \texttt{Nodo IA}, la distribución de los directorios y los ficheros desarrollados para llevar a cabo el proyecto, cuyo contenido se puede ver además en el~\textbf\textit{{\nameref{sec:anexoB}}}. Como se observa en la disposición jerárquica de la ilustración, se separan las funciones, por ejemplo, los despliegues de \textit{Docker} se centralizan en un fichero \texttt{docker-compose.yml} en el directorio \texttt{chat-feats}, los modelos de IA para definir en Ollama se guardan en el directorio \texttt{modelos\_ia}, los \textit{pipelines} tienen su propio directorio \texttt{openwebui\_pipelines}, sincronizado con el directorio de \textit{pipelines} de dentro del contenedor, y todos los scripts de \textit{Python} se alojan en el directorio \texttt{python-scripts}, separando el módulo de ingestión de datos en \texttt{opencti}, el motor de RAG en \texttt{rag}, y finalmente, el programa principal que pone en marcha el motor, \texttt{main.py}.
  
\subsubsection{Consumo y vectorización de OpenCTI}
\subsubsubsection{Ingestión}
El cometido de este módulo es conectarse a OpenCTI mediante el SDK (provisto por el paquete oficial \textit{pycti}), configurando los credenciales adecuados (\textit{IP} de \textit{OpenCTI} y clave de la API). El cliente es pesado, y para evitar ser cargado múltiples veces, por ejemplo, por diferentes hilos de ejecución, ralentizando el sistema completo, sigue un patrón \textit{Singleton}.

Para cada tipo de entidad CTI que se cargará (concretamente \textit{threat actors, malware, attack patterns, indicators y reports}), el módulo define qué atributos necesita recuperar. Para todos los tipos de objeto se cargan identificador, nombre, descripción y fecha de creación. Para \textit{Threat Actors} y \textit{Reports}, estos son los únicos campos recuperados. Se recuperan campos adicionales para:
\begin{itemize}
	\item \textbf{Malware:} Para este grupo se recupera también el campo \texttt{malware\_types}, que define a qué categorías de malware pertenece (e.g. \textit{ransomware}, \textit{trojan}, \textit{backdoor})
	\item \textbf{Attack Patterns:} Para este grupo se recupera además el campo \texttt{x\_mitre\_id}, que es el identificador MITRE asociado a una técnica cuando el \textit{Attack Pattern} pertenece al modelo MITRE ATT\&CK\cite{attck}
	\item \textbf{Indicators:} Se cargan adicionalmente los campos \texttt{pattern\_type} y \texttt{pattern}, que indican la sintaxis en la que está escrito el campo \texttt{pattern}, y la expresión técnica que define el \textit{IoC}. Esto es, el valor y la forma asociado al indicador (por ejemplo, un dominio cuyo valor es \textit{officeonlaine.com}).
\end{itemize}
 Así, no se descarga todo el objeto CTI, ya que esto sería un proceso muy largo y pesado, sino sólo los aspectos relevantes para las fases posteriores del procesado.

Los datos se recuperan mediante ingesta paginada, consumiendo todas las páginas devueltas por \textit{OpenCTI} hasta que no quedan más disponibles. Esto se debe a que OpenCTI puede tener del orden de cientos de miles o incluso millones de entidades, por lo que una única llamada a la API no sería suficiente para recuperar todos los datos o, según la configuración de \textit{timeouts}, podría no devolver nada. 

Una vez finalizada la extracción, guarda los datos obtenidos en ficheros \texttt{JSON} independientes. Por ejemplo, \texttt{threat\_actors.json} o \texttt{indicators.json}.  En este caso, se guardan en el directorio \texttt{/opt/data/raw/opencti}. Como referencia, una ejecución de este programa, concretamente el 8 de junio de 2026,  carga 55 \textit{threat actors}, 355 \textit{malwares}, 410 \textit{attack patterns}, 515569 \textit{indicators} y 5924 \textit{reports}.

Todo este proceso lo lleva a cabo el script \texttt{opencti/ingest.py}.

\subsubsubsection{Normalización}
Este modulo se encarga de la normalización de los datos brutos extraídos de \textit{OpenCTI}. Transforma los objetos recuperados en documentos textuales homogéneos adecuados para su posterior vectorización. Los datos extraídos de \textit{OpenCTI} se almacenan como JSON bruto, por lo que cada objeto recuperado mantiene su estructura original. Esa representación no es la más adecuada para realizar búsquedas semánticas, ya que los modelos de lenguajes trabajan mejor sobre texto descriptivo que sobre JSON complejos. 

Es una capa intermedia entre la ingesta bruta y la generación de \textit{embeddings} para contener, para cada objeto CTI, la información relevante en un formato legible, y más importante, igual para todos los objetos de un mismo tipo para facilitar la búsqueda por parte del LLM al vectorizar el texto. Aunque indicadores y reportes se dejan tal y como están, se incluyen en esta parte la búsqueda de relaciones de cierto tipos de objetos en \textit{OpenCTI} para mejorar la semántica asociada al mismo:
\begin{itemize}
	\item \textbf {Threat Actors:} se incluyen relaciones tipo \texttt{uses} para recuperar \textit{Attack Patterns} usados y \textit{Malware} asociado.
	\item \textbf{Malware:} se incluyen también relaciones tipo \texttt{uses} para recuperar \textit{Attack Patterns} usados.
	\item \textbf{Attack Patterns: } se incluyen relaciones tipo \texttt{uses} para recuperar \textit{Threat Actors} que usan estos patrones y técnicas.
\end{itemize}

El resultado final del proceso de normalización son documentos estructurados, uniformes para cada tipo de objeto recuperado de \textit{OpenCTI}. Por razones de procesado y eficiencia, se guardarán en formato \texttt{JSONL} (\textit{JSON Lines}), que almacena un objeto JSON por linea, aunque extrayendo el contenido y dejandolo en un formto humanamente legible, quedaría como los ejemplo en el Listado~\ref{lst:ejemplos}.

Esta parte del proceso es llevada a cabo por los scripts \texttt{normalizer}, que define los procesos de normalización para cada tipo de objeto y \texttt{normalize\_all.py}, que lee los ficheros \texttt{JSON} guardados, procesa la normalización de cada uno de estos ficheros y los guarda en su correspondiente fichero \texttt{JSONL}.

\lstinputlisting[inputencoding=utf8/latin1, breaklines=true,caption=Ejemplos de estructuras normalizadas,label=lst:ejemplos]{ejemplos_normalized}

\subsubsubsection{Generación de \textit{embeddings} e inserción en \textit{ChromaDB}}
Tras la fase de normalización, los objetos CTI extraídos desde OpenCTI se encuentran representados como documentos textuales semiestructurados. Aunque estos documentos ya son legibles y contienen información relevante sobre actores, malware, técnicas, indicadores e informes, todavía no pueden ser utilizados directamente para realizar búsquedas semánticas. Para ello es necesario transformarlos en representaciones vectoriales, conocidas como \textit{embeddings}, que permiten comparar documentos y consultas en función de su similitud semántica.

El proceso de vectorización consiste en cargar los ficheros \texttt{.jsonl} generados durante la normalización, extraer el campo \texttt{text} de cada documento y convertirlo en un vector numérico mediante el modelo \texttt{sentence-transformers/all-MiniLM-L6-v2}. Este modelo genera una representación densa del significado del texto, de forma que documentos conceptualmente similares quedan próximos dentro del espacio vectorial. En paralelo, también se cargan los ficheros de metadatos asociados a cada colección, comprobando que el número de textos y metadatos coincide antes de generar los embeddings. Esta comprobación es importante para garantizar que cada vector queda asociado al documento y metadatos correctos.

Los embeddings generados se almacenan en ficheros \texttt{.npy}, separados por tipo de entidad. Así, se generan ficheros independientes para \texttt{threat\_actors}, \texttt{malware}, \texttt{attack\_patterns}, \texttt{reports} e \texttt{indicators}. Esta separación permite mantener una organización coherente del corpus CTI y facilita la carga posterior en colecciones diferenciadas dentro de ChromaDB. Además, guardar los embeddings como ficheros \textit{NumPy} permite reutilizarlos sin necesidad de recalcularlos cada vez que se quiera reconstruir la base vectorial.

Una vez generados los embeddings, el siguiente paso consiste en cargarlos en ChromaDB. Para ello, el script de inserción se conecta al servidor de ChromaDB mediante \texttt{HttpClient}. Para cada tipo de entidad, el script crea o recupera una colección con el mismo nombre de la entidad, por ejemplo \texttt{threat\_actors}, \texttt{malware}, \texttt{attack\_patterns}, \texttt{reports} o \texttt{indicators}. Las colecciones se configuran utilizando similitud coseno, una métrica habitual para comparar embeddings en sistemas de recuperación semántica.

Antes de insertar los documentos en ChromaDB, el script valida que los textos normalizados, los metadatos y los embeddings tengan la misma longitud. Esta validación evita desalineaciones entre documentos, vectores y metadatos. Posteriormente, la inserción se realiza por lotes, asociando a cada documento su identificador, su texto, su embedding y sus metadatos. De este modo, cada entrada almacenada en ChromaDB conserva tanto la representación vectorial necesaria para la búsqueda semántica como la información textual y de trazabilidad necesaria para interpretar el resultado recuperado.

El resultado final de este proceso es una base vectorial organizada en colecciones CTI, donde cada documento procedente de OpenCTI puede ser recuperado por similitud semántica. Durante una consulta, el backend RAG genera un embedding de la pregunta o del indicador detectado y consulta las colecciones de ChromaDB para obtener los documentos más cercanos. Estos documentos se incorporan al contexto final como información contextual o inferida, diferenciándola de la inteligencia explícita recuperada directamente desde OpenCTI.

La vectorización y carga en ChromaDB son, por tanto, una parte esencial del sistema RAG. Esta fase permite complementar la información confirmada de OpenCTI con contexto semántico adicional, como actores, malware, técnicas, informes o indicadores relacionados conceptualmente con la consulta. Sin embargo, la similitud vectorial no implica una relación confirmada entre entidades CTI. Por este motivo, los documentos recuperados desde ChromaDB se tratan como contexto inferido y se separan explícitamente de la información considerada confirmada.

Este proceso lo llevan a cabo los scripts \textit{embed\_all\_st.py}, para la generación de \textit{embeddings}, y \textit{chroma\_load.py}

\begin{figure}[h!]
	\centering
	\begin{tcolorbox}[
		colback=gray!5,                    % Fondo gris suave
		colframe=gray!50,                  % Borde del recuadro
		arc=0mm,                           % Esquinas rectas
		boxrule=0.5pt,                     % Grosor del borde
		width=0.95\linewidth,              % Ancho del contenedor
		coltitle=black,
		colbacktitle=gray!15,              % Fondo de la cabecera
		fonttitle=\small\bfseries,
		sharp corners,
		before skip=10pt,                  % Espaciado antes del recuadro
		after skip=10pt                    % Espaciado después del recuadro
		]
		\dirtree{%
			.1 /opt.
			.2 chat-feats.
			.3 docker-compose.yml.
			.2 modelos\_ia.
			.3 all-MiniLM-L6-v2.
			.3 cyberpal.
			.4 Modelfile.
			.3 lily.
			.4 Modelfile.
			.2 ollama.
			.2 openwebui\_data.
			.2 openwebui\_pipelines.
			.3 cybeross\_pipeline.
			.3 lily\_pipeline.
			.3 rag\_pipeline.
			.3 cybeross\_pipeline.py.
			.3 lily\_pipeline.py.
			.2 python-scripts.
			.3 opencti.
			.4 chroma\_load.py.
			.4 client.py.
			.4 embed\_all\_st.py.
			.4 ingest.py.
			.4 normalize\_all.py.
			.4 normalizer.py.
			.4 rag\_client.py.
			.3 rag.
			.4 context.
			.5 builder.py.
			.5 context\_profiles.py.
			.4 app.py.
			.4 ioc\_context\_builder.py.
			.4 ioc\_resolver.py.
			.4 query\_classifier.py.
			.3 main.py.
			.3 requirements.txt.
		}
	\end{tcolorbox}
	\caption{Estructura de directorios del proyecto}
	\label{fig:tree}
\end{figure}

\subsubsection{Frontend, backend y motor de RAG}
Se puede entender el workflow anterior como una tarea a realizar con la instalación, para el primer poblado de la base de datos vectorial, y periódicamente para mantener el sistema actualizado respecto al conocimiento de OpenCTI. Así, este segundo workflow, que se puede ver etiquetado con números arábigos del 1 al 6 en la Figura~\ref{fig:despliegue} y en más detalle en la Figura~\ref{fig:secuencia}
es el caso de uso más común para esta aplicación, en el cual el analista haría una pregunta en la interfaz web seleccionando el modelo que quiere que responda, y espera a obtener un análisis del indicador por el que ha preguntado.

\subsubsubsection{\OpenWebUI y servidor \pipelines}
Estas dos partes se consideran parte del mismo sistema, pese a ser realmente disjuntas y ser distribuibles (\OpenWebUI al servidor \pipelines\ a través de una IP remota), dado que el servidor \pipelines\ es un plugin del ecosistema \OpenWebUI, el cual se conecta a dicho servidor y permite mostrar y configurar las \textit{pipelines} configuradas en la propia interfaz.

\OpenWebUI es una interfaz web conversacional. Su función es proporcionar al usuario un entorno web desde el que formular preguntas sobre indicadores de compromiso y visualizar las respuestas generadas por los modelos. \OpenWebUI no contiene contiene lógica de recuperación de contexto y no accede directamente a \textit{OpenCTI} o \textit{ChromaDB}, dado que no puede hacerlo de forma nativa (sí que tiene una funcionalidad de base de conocimiento, pero esta se basa en la subida de archivos \textit{PDF} o texto plano, e incluso búsqueda en la web, pero no en la consulta de APIs privadas), por lo que delega esa función en el servidor \pipelines.

El despliegue de \OpenWebUI\ se realiza mediante \textit{Docker} , que facilita su ejecución aislada y su integración con el resto de componenetes del sistema. Dentro de la configuración de este contenedor (que se puede consultar como \textit{docker-compose.yml} en el~\textbf{\textit{\nameref{sec:anexoB}}}). Se puede configurar la dirección de \textit{Ollama} con la variable \texttt{OLLAMA\_HOST}, permitiendo la consulta directa de los modelos listados por \textit{Ollama}, pero sin RAG. Adicionalmente, accediendo como usuario administrador (que en este caso, como es una plataforma expuesta de forma privada a un equipo concreto, puede ser accesible para todo el mundo configurando la variable \texttt{WEBUI\_AUTH} como \texttt{false}), yendo al \textit{Admin Panel}, después a los ajustes (\textit{Settings}) y a \textit{Pipelines}, se puede configurar la conexión con el servidor \pipelines. En este caso, como está dentro de la misma red \textit{Docker} con el nombre \texttt{pipelines}, será \texttt{http://pipelines:9099}. El flujo de un analista sería simplemente, en la interfaz web, seleccionar el modelo que quiere que conteste, escribir la pregunta y aguardar la respuesta.

El servidor \pipelines\ es el componente que transforma \OpenWebUI\ en más que una interfaz genérica, permitiendo desarrollar todo tipo de integraciones. Las \textit{pipelines} se definen como scripts \textit{Python} cargados por el servidor de \pipelines\ de \OpenWebUI. Cada \textit{pipeline} contiene una clase principal \texttt{Pipeline} que implementa el método \texttt{pipe}, y una clase \texttt{Valves}, que permite definir parámetros configurables desde la interfaz. Estos parámetros pueden visualizarse y modificarse desde \OpenWebUI, que permite ajustar el comportamiento de la \textit{Pipeline} sin modificar directamente el código.

En \OpenWebUI, las \textit{pipelines} se utilizan como si fueran modelos seleccionables. Es decir, el usuario no selecciona necesariamente un modelo de lenguaje puro, sino una \textit{pipeline} que aparece en la interfaz como una opción más de conversación. Sin embargo, initernamente, la \textit{pipeline} no envía la pregunta directamente al modelo, sino que intercepta la consulta, ejecuta lógica personalizada, solicita contexto al backend RAG y finalmente construye un \textit{prompt} enriquecido para el modelo local.

Se han definido dos \textit{pipelines}: \texttt{Lily RAG} y \texttt{CyberOSS RAG}. Ambas comparten la misma idea general: reciben la pregunta del usuario desde \OpenWebUI, extraen el último mensaje de tipo \texttt{user}, llaman al backend RAG para recuperar contexto CTI y posteriormente envían la pregunta enriquecida al modelo correspondiente mediante \textit{Ollama}. La diferencia entre ambas \textit{pipelines} radica en el modelo utilizado, el formato concreto de llamada al modelo y el perfil de contexto utilizado.

La clase \texttt{Valves} actúa como mecanismo de configuración. En la \textit{pipeline} de Lily se definen por defecto el modelo \texttt{lily-cyber} y  el perfil de contexto \texttt{small}, mientras que en el \textit{pipeline} de CyberOss se definen el modelo \texttt	{cybeross} (estos nombres son los identificadores que se les da en \textit{Ollama}) y un perfil de contexto más amplio. Más adelante se comentarán en detalle estos perfiles de contexto.

El método \texttt{pipe} constituye el punto de entrada a la pipeline, es el método que \OpenWebUI\ llama, enviándole todo el cuerpo de la conversación, que contiene los mensajes intercambiados. La \textit{pipeline} extrae los mensajes del parámetro \texttt{kwargs} y delega la ejecución principal en el método \texttt{run}. Después devuelve el resultado, usando la palabra clave \texttt{yield}. Esto se debe a que \texttt{yield} convierte la función en un generador, que devuelve valores de forma iterable y permite que quien llama vaya consumiendo la salida. Este es el comportamiento \OpenWebUI\ espera.

Ambas \textit{pipelines} incluyen comprobaciones para detectar tareas interans de \OpenWebUI, que suele lanzar peticiones adicionales al modelo (o \textit{pipelines}) seleccionados para rellenar datos como nombre, etiquetas o resumen del chat o posibles preguntas de \textit{follow-up} para seguir la conversación. Recibir estas tareas además de las preguntas sobre CTI satura a los modelos, se invierte mucho tiempo y recursos y pueden saltar \textit{timeouts} o resultados inesperados, así que cuando la \textit{pipeline} recibe esta clase de peticiones utiliza salvaguardas, como devolver respuestas mínimas, y evita ejecución de recuperación de contexto inútil.

En cambio, si se recibe una pregunta real sobre CTI del usuario, la pipeline envía una petición al \textit{endpoint} \texttt{/query-context} del backend, con la pregunta del usuario y el tamaño del contexto a recuperar. La lógica completa de este backend será explicada en la siguiente sección. Una vez que se devuelve el contexto estructurado, lo incorpora al prompt, el cual varía entre \textit{pipelines}:
\begin{itemize}
	\item \textbf{Lily}: En esta \textit{pipeline}, se pide como mensaje de sistema, que el modelo actúe como un analista CTI, utilice toda la información que se le proporciona, extraiga indicadores y hechos explícitos, separe inteligencia factual (extraída de \textit{OpenCTI}) de información inferida obtenida de similitud vectorial en \textit{ChromaDB}.
	 \item \textbf{CyberOSS}: el flujo general de la pipeline es similar, se le dan las mismas instrucciones que a \textit{Lily}, pero incorpora una adaptación específica de formato de \textit{prompt}. Durante las pruebas se observó que \textit{CyberOSS} no respondía correctamente con el formato estándar de chat utilizado por otros modelos (como \textit{Lily}) en \textit{Ollama}. Esto se debe a que utiliza unos tokes específicos (\texttt{<|end|><|start|>assistance} o \texttt{<|start|>user}) para delimitar las preguntas del usuario y las respuestas del asistente. 
\end{itemize}

Las \textit{pipelines} se definen en el directorio \texttt{openwebui\_pipelines}, que se sincroniza con el directorio interno del contenedor \textit{pipelines} al reiniciar este último, cargando las \textit{pipelines} y mostrándolas en la interfaz de \OpenWebUI\ si son correctas, o moviéndolas a un nuevo directorio \texttt{failed} si contienen errores.

\subsubsubsection{Backend y motor RAG}
El backend constituye el componente encargado de generar el contexto que posteriormente será entregado al modelo de lenguaje. A diferencia de las \textit{pipelines}, este backend no se encarga de comunicarse con los modelos, sino que actúa como un servicio especializado de recuperación y construcción de contexto. Su función es recibir la consulta del usuario, extraer el indicador por el que está preguntando, recuperar información explícita de \textit{OpenCTI}, obtener contexto adicional de \textit{ChromaDB} y devolver un bloque de contexto estructurado.

Se implementa sobre \textit{FastAPI} y expone el \textit{endpoint}. Este endpoint recibe una petición (método \texttt{POST}) con al menos dos campos: la propia petición (de ahora en adelante \textit{query}, en el campo homónimo) y en el campo \texttt{context\_profile} recibe el perfil de contexto que define el nivel de detalle de la información a recuperar. Este diseño permite que los pipelines seleccionen más o menos contexto según el modelo que lo vaya a procesarlo.

El flujo general del backend puede resumirse de la siguiente manera:
\begin{enumerate}
	\item Pregunta del usuario
	\item Extracción del IOC
	\item Resolución explícita del IOC en \textit{OpenCTI}
	\item Búsqueda semántica en \textit{ChromaDB}
	\item Construcción del contexto final
	\item Devolución del contexto a la \textit{pipeline}
\end{enumerate}
El backend se mantiene desacoplado de la inferencia del modelo. Esta separación permite usar el mismo backend, mismos endpoints, con diferentes modelos y \textit{pipelines}.

El fichero \texttt{app.py} define el servicio \textit{FastAPI} y define el \textit{endpoint} principal del backend. Al recibir una petición en \texttt{/query-context}, el backend valida que el cuerpo recibido sea \texttt{JSON} y y que contenga una pregunta válida. Si esto no se cumple, se devuelve un error controlado \texttt{422}. Una vez validada la entrada, se obtiene el perfil de contexto seleccionado. En caso de estar vacío, la opción por defecto es \texttt{small}. Este perfil determina cuántos documentos contextuales se recuperan y con qué nivel de detalle se incluirán en el contexto final.

A continuación, se intenta extraer un indicador de compromiso de la pregunta utilizando el módulo \texttt{ioc\_resolver.py}. Si no se detecta ningún \texttt{IoC} válido, el backend devuelve un error indicando que no se ha encontrado un indicador en la pregunta.  Para detectar los indicadores, este módulo utiliza matching de expresiones regulares. Esto se debe a que los indicadores suelen seguir formatos claramente identificables. Por ejemplo, una dirección IPv4, una URL, un dominio o un hash tienen patrones sintácticos bien definidos(4 números del 0/1 al 255 separados por puntos, caracteres hexadecimales un numero determinado de veces...). Esta aproximación resulta más simple, rápida y controlable que usar un modelo de lenguaje o un clasificador para detectar indicadores. El orden de detección es el siguiente:
\begin{enumerate}
	\item \textbf{URL}. es importante que vaya antes que los dominios para evitar que una URL sea etiquetada parcialmente como un dominio.
	\item \textbf{SHA-256}
	\item \textbf{SHA-1}
	\item \textbf{MD5}
	\item \textbf{IPv4}
	\item \textbf{Dominio}
	\end{enumerate}
	Una vez detectado el \textit{IoC}, el módulo \texttt{resolve\_ioc} consulta \textit{OpenCTI} mediante el cliente \texttt{rag\_client.py}, una encapsulación del cliente normal (\texttt{client.py}) de \textit{OpenCTI} para hacer consultas específicas de RAG. Esta resolución obtiene tres tipos de información: \texttt{direct\_indicators}, que proceden de búsquedas por valor, \texttt{pattern\_matches}, que filtran aquellos indicadores cuyo campo \texttt{pattern} contiene el \textit{IoC} y \texttt{related\_reports}, que permiten recuperar informes que mencionan el indicador. Esta información se considera la parte fuerte o explícita del contexto, ya que procede directamente de \textit{OpenCTI}.

A continuación, se construye un texto legible mediante \texttt{build\_ioc\_context}. Este módulo transofmra el objeto recuperado desde \textit{OpenCTI} en un bloque de texto estructurado y legible, convirtiendo datos técnicos en una representación útil para análisis CTI. Combina los indicadores recuperados por búsqueda diredcta y por coincidencia de patrón. Aplica una deduplicación por clave (valor del indicador, estado, \textit{confidence} y patrón STIX) para evitar mostrar el mismo indicador varias veces en el contexto. Esto se debe a que durante las pruebas se observó que la aparición de elementos duplicados en el contexto tiende a confundir a modelos pequeños, empobreciendo sus respuestas. Cada indicador se muestra junto a sus campos más relevantes, como valor, tipo, estado (activo/revocado), puntuación, patrón, descripción, validez, u origen.

Además de la consulta a \textit{OpenCTI}, el backend realiza una recuperación semántica contra \textit{ChromaDB}. Para ello, \texttt{app.py} utiliza el modelo \texttt{all-MiniLM-L6-v2} del framework \texttt{sentence-transformers} para convertir la consulta en un \textit{embedding}. Este \textit{embedding} se consulta contra varias colecciones vetoriales:
\begin{itemize}
	\item \texttt{threat\_actors}
	\item  \texttt{malware}
	\item \texttt{attack\_patterns}
	\item \texttt{reports}
	\item \texttt{indicators}
\end{itemize}
Para cada colección, el backend recupera los documentos más cercanos según similitud vectorial. Estos documentos proceden del proceso de ingesta de \textit{OpenCTI}. Esta recuperación permite aportar contexto adicional al modelo que pordía ser semánticamente relevante para la consulta, pero es importante recalcar que esta similitud no equivale a una relación confirmada. Es por esto que esta parte del contexto aparece diferenciada explícitamente de la inteligencia fuerte. 

El contexto es filtrado de acuerdo a los ya mencionados perfiles de conexto (definidos en \texttt{context\_profiles.py}). Estos perfiles controlan cuántos documentos semánticos se incluyen en la respuesta y con qué nivel de detalle se presentan. El perfil \texttt{small} está pensado para modelos pequeños con poca capacidad de contexto, como \textit{Lily}. Limita el número de documentos inferidos y utiliza una versión compacta de los mismos. Presenta tipo de objeto recuperado, nombre, descripción y fecha de creación. Aunque es algo pobre, puede aportar algo de utilidad como contexto superficial. El contexto \texttt{large} incluye más documentos y mantiene el contenido completo. Está pensado para modelos de mayor capacidad como \textit{CyberOSS}., que pueden procesar más contexto y extraer relaciones más complejas. Inicialmente no se planteó la utilización de perfiles de contexto, pero se introdujo al realizar pruebas con \textit{Lily} y ver que el modelo daba respuestas pobres y apenas analizaba el contexto que se le daba. Si el conexto es demasiado grande, puede incluso no responder por cumplirse el \textit{timeout}.

El módulo \texttt{builder.py} se encarga de unir el contexto fuerte extraído de \textit{OpenCTI} con el contexto inferido recuperado de \textit{ChromaDB}. Es el último paso antes de devolver el contexto a la \textit{pipeline}. El módulo recibe tres elementos:
\begin{itemize}
	\item \texttt{ioc\_context\_text}: contiene la información explícita del \textit{IoC}.
	\item \texttt{context\_docs}: contienen documentos recuperados por similitud semántica.
	\item \texttt{context\_profile}.
\end{itemize}
 Finalmente, a función \texttt{\_prepare\_conextual\_docs} aplica el nivel de detalle solicitado según el perfil de contexto y \texttt{\\build\_final\_context} construye el bloque de contexto final. El el Listado~\ref{lst:ctx} se pueden ver dos ejemplos de contexto recuperado y devuelto a la \textit{pipeline}, tanto grande como pequeño para el indicador conocido \textit{'officeonlaine.com'}.
 
 \lstinputlisting[language=Python, inputencoding=utf8/latin1,
 breaklines=true,caption=Contextos para 'officeonlaine.com',label=lst:ctx]{context.txt}
 
\begin{figure}[] % Cambiado a H (mayúscula) para fijar la posición con el paquete 'float'
	\centering
	\includesvg[width=\textwidth,inkscapelatex=false]{Imagenes/secuencia.svg}
	\caption{Workflow completo de consulta}
	\label{fig:secuencia}
\end{figure}


% RESTRICCIONES: 
% - El analista está obligado a preguntar por un indicador concreto, si no el sistema no sabe qué buscar de forma sólida y sólo podría consultar la base de datos vectorial. Para que esto fuera útil, necesitaríamos modelos mucho más potentes y una base de datos vectorial mucho más rica, y por ende, más recursos. Esto sería útil si fuera una línea de negocio importante que requiriera un proceso de negocio bueno, y por ende se destinaran más recursos.
% Explicar los diferentes modos de contexto, bla bla bla

\newpage
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%% Evaluación y discusión de resultados
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Evaluación y discusión de resultados}

Para comprobar el rendimiento real del sistema bajo restricciones de hardware (ejecución exclusiva en CPU y memoria RAM), se realizaron pruebas empíricas basadas en siete tipos de consultas. Las respuestas de los modelos se evaluaron mediante una rúbrica cualitativa de seis criterios técnicos, puntuados de 0 a 2. Todos los volcados de las conversaciones y el desglose de estas calificaciones se encuentran detallados en el \textbf{\textit{\nameref{sec:anexoC}}}.

A nivel cuantitativo, los tiempos de inferencia se resumen en la Tabla~\ref{tab:tiempos}. Como era de esperar, el modelo \textit{Lily} (7B) resulta mucho más rápido, con una media de 01:34,5 por respuesta, frente a los 03:07,2 de CyberOSS (20B). Este resultado responde a un hecho físico evidente en entornos \textit{on-premise}: al no contar con aceleración por GPU, el factor limitante es la velocidad de la memoria RAM para leer los pesos del modelo. Al ser el archivo de \textit{CyberOSS} cuatro veces más pesado que el de \textit{Lily}, la transferencia de datos ralentiza notablemente la velocidad de generación, un coste que el analista debe valorar a cambio de una mayor capacidad de análisis.

Sin embargo, al observar las desviaciones típicas registradas en la Tabla~\ref{tab:tiempos} (0,000361 para \textit{Lily} y 0,000667 para \textit{CyberOSS}), se constata de forma matemática que la dispersión de los tiempos de respuesta no es exagerada en ninguno de los dos casos. Los valores de desviación tan extremadamente bajos indican que el comportamiento de los motores de inferencia local sobre CPU es altamente predecible y estable. No existen picos de retraso anómalos o aleatorios entre consultas pesadas y ligeras; el servidor mantiene una latencia de cómputo uniforme donde el tiempo de ejecución escala de manera directamente proporcional al tamaño del prompt y al número de tokens generados, sin verse penalizado por problemas de concurrencia o degradación térmica del hardware.

En el plano cualitativo, los resultados recogidos en la Tabla~\ref{tab:medias} confirman una máxima conocida en el ámbito de los Grandes Modelos de Lenguaje: un modelo es tan bueno como su entrenamiento previo y el contexto que se le proporciona. Aquí, \textit{CyberOSS} (20B) demostró una consistencia superior gracias a su mayor tamaño y a un ajuste fino más robusto.

% Tabla para el dominio conocido
\begin{table}[h]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 1 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 1 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 0 & 2 \\
		\hline
		¿Separa relaciones fuertes y débiles?  & 1 & 2 \\
		\hline
		¿Alucina? & 1 & 1 \\
		\hline
		¿Es útil para análisis? & 1 & 2 \\
		\hline
		Puntuación & 5 & 11 \\
		\hline
	\end{tabular}
	\caption{Resultados para dominio conocido}
	\label{tab:dominio}
\end{table}

%Tabla para IP conocida
\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 1 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 1 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 2 & 1\\
		\hline
		¿Separa relaciones fuertes y débiles?  & 1 & 1\\
		\hline
		¿Alucina? & 1 & 2 \\
		\hline
		¿Es útil para análisis? & 1 & 1 \\
		\hline
		Puntuación & 7 & 9 \\
		\hline
	\end{tabular}
	\caption{Resultados para dirección IP conocida}
	\label{tab:ip}
\end{table}

% Tabla para hash conocido
\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 2 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 2 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 0 & 2 \\
		\hline
		¿Separa relaciones fuertes y débiles?  & 1 & 2 \\
		\hline
		¿Alucina? & 2 & 1 \\
		\hline
		¿Es útil para análisis? & 1 & 2 \\
		\hline
		Puntuación & 8 & 11 \\
		\hline
	\end{tabular}
	\caption{Resultados para hash conocido}
	\label{tab:hash}
\end{table}

% Tabla para pregunta por contexto inferido
\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 1 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 0 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 1 & 2 \\
		\hline
		¿Separa relaciones fuertes y débiles?  & 0 & 1 \\
		\hline
		¿Alucina? & 1 & 1 \\
		\hline
		¿Es útil para análisis? & 1 & 2 \\
		\hline
		Puntuación & 4 & 10 \\
		\hline
	\end{tabular}
	\caption{Resultados para pregunta por contexto inferido}
	\label{tab:contexto}
\end{table}

Esta diferencia se observa claramente en las consultas de datos fijos individuales, como la "Pregunta sobre dominio conocido" (Tabla~\ref{tab:dominio}), "Pregunta sobre IP conocida" (Tabla~\ref{tab:ip}) y "Pregunta sobre hash conocido" (Tabla~\ref{tab:hash}). \textit{CyberOSS} obtuvo las puntuaciones más altas al procesar el contexto sin problemas y estructurar la información de \textit{OpenCTI} de forma clara. En cambio, \textit{Lily} se vio penalizado en las Tablas 1 y 3. Esto ocurrió porque, al configurarse con un perfil de contexto más reducido (\textit{small}), la falta de datos suficientes provocó que el modelo recurriera a recomendaciones genéricas y respuestas estándar en lugar de ofrecer datos específicos del caso.

Por otro lado, las pruebas también reflejan otro comportamiento habitual en los LLM: funcionan mucho mejor cuando se les piden tareas concretas y bien dirigidas, en lugar de peticiones generales. El ejemplo más claro se da al comparar la "Pregunta sobre IP conocida" (Tabla~\ref{tab:ip}) con la "Pregunta abierta" (Tabla~\ref{tab:abierta}). A pesar de trabajar sobre el mismo indicador y con el mismo volumen de datos, Lily mejoró su puntuación de 7 a 11 puntos en la pregunta abierta. La razón es que la instrucción de la Tabla le ordenaba una tarea más específica ("Summarize information about..."), lo que ayudó al modelo de menor capacidad a estructurar la respuesta y a separar de forma clara los datos reales del contexto de sus propias conclusiones, sacando un 11 aun siendo un modelo pequeño (misma puntuación que CyberOSS), frente a un 7 al preguntar por IP.

% Tabla de relación con actores
\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 1 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 0 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 1 & 2 \\
		\hline
		¿Separa relaciones fuertes y débiles?  & 0 & 2 \\
		\hline
		¿Alucina? & 0 & 2 \\
		\hline
		¿Es útil para análisis? & 0 & 2 \\
		\hline
		Puntuación & 2 & 12 \\
		\hline
	\end{tabular}
	\caption{Resultados de relación con actores}
	\label{tab:actores}
\end{table}

% Tabla para separación fuerte/débil explícita
\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 2 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 1 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 2 & 2 \\
		\hline
		¿Separa relaciones fuertes y débiles?  & 2 & 1 \\
		\hline
		¿Alucina? & 2 & 1 \\
		\hline
		¿Es útil para análisis? & 2 & 2 \\
		\hline
		Puntuación & 12 & 10 \\
		\hline
	\end{tabular}
	\caption{Resultados de separación fuerte/débil explícita}
	\label{tab:fuerte_debil}
\end{table}

% Tabla para pregunta abierta
\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		¿Menciona indicadores explícitos? & 2 & 2 \\
		\hline
		¿Muestra bien el estado y confidence? & 2 & 2 \\
		\hline
		Menciona y separa el contexto inferido & 2 & 2 \\
		\hline
		¿Separa relaciones fuertes y débiles?  & 2 & 2 \\
		\hline
		¿Alucina? & 2 & 1 \\
		\hline
		¿Es útil para análisis? & 2 & 2 \\
		\hline
		Puntuación & 11 & 11 \\
		\hline
	\end{tabular}
	\caption{Resultados de pregunta abierta}
	\label{tab:abierta}
\end{table}

Esta dependencia del contexto y del guiado se hizo aún más evidente en la prueba de "Relación con actores" (Tabla~\ref{tab:actores}). Al enfrentarse a una consulta compleja, Lily cometió un error clásico de los modelos pequeños: al ver nombres de atacantes dentro del contexto recuperado, asumió por simple proximidad que existía una relación directa y confirmada, cayendo en una alucinación analítica. Por el contrario, CyberOSS demostró un mejor entrenamiento al identificar correctamente que, según las directrices del sistema y los datos de ChromaDB, no había ninguna prueba explícita de vinculación, clasificando la relación como una mera posibilidad teórica.

\shorthandoff{"}
Finalmente, en la pregunta sobre "Contexto inferido" (Tabla~\ref{tab:contexto}) se aprecia cómo impacta la densidad del contexto en modelos de diferente escala. Ante una consulta que inyecta un volumen masivo de indicadores y reportes secundarios de \textit{OpenCTI}, la atención de Lily se satura por completo, haciéndole ignorar elementos clave de la consulta y desplomando su nota a un 4/12. En cambio, CyberOSS demuestra su resiliencia y capacidad de procesamiento manteniendo un sólido 10/12 en la Tabla 4. Aunque el modelo de 20B parámetros asume de forma un poco rígida que no hay más información relevante debido al ruido de los datos secundarios, su entrenamiento le permite discriminar el contexto e impedir que la sobrecarga de información degrade su calidad analítica de forma drástica como le ocurre al modelo ligero.
\shorthandon{"}

\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		& Lily & CyberOSS \\
		\hline
		Promedio & 6,86 & 10,57 \\
		\hline
		Sobre 10 & 5,71& 8,81 \\
		\hline
	\end{tabular}
	\caption{Resultados finales}
	\label{tab:medias}
\end{table}

\begin{table}[]
	\centering
	\begin{tabular}{||c|c|c||}
		\hline
		Pregunta & Lily & CyberOSS  \\
		\hline
		Pregunta sobre dominio conocido & 01:49,2 & 03:17,3 \\
		\hline
		Pregunta sobre IP conocida & 01:16,6 & 02:20,5 \\
		\hline
		Pregunta sobre hash conocido & 01:29,7 & 02:45,0 \\
		\hline
		Pregunta abierta & 02:05,5 & 04:21,4 \\
		\hline
		Pregunta por contexto inferido & 01:43,8 & 02:37,0 \\
		\hline
		Separación fuerte/débil explícita & 00:35,3 & 02:01,6 \\
		\hline
		Relación con actor & 02:01,5 & 04:27,4 \\
		\hline
		\hline
		Mejor tiempo & 00:35,3 & 02:01,6 \\
		\hline
		Peor tiempo & 02:05,5 & 04:27,4 \\
		\hline
		Rango & 01:30,2 & 02:25,7 \\
		\hline
		Media & 01:34,5 & 03:07,2 \\
		\hline
		Mediana & 01:43,8 & 02:45,0 \\
		\hline
		Desviación típica & 0,000361 & 0,000667 \\
		\hline
	\end{tabular}
	\caption{Comparativa de tiempos de respuesta}
	\label{tab:tiempos}
\end{table}

En conclusión, la evaluación comparativa revela un claro balance técnico entre coste de cómputo y calidad analítica: la gran fortaleza de \textit{Lily} (7B) radica en su velocidad y agilidad bajo recursos estrictos de CPU, pero su debilidad es la tendencia a alucinar relaciones por proximidad semántica ante consultas abstractas o complejas; por el contrario, la fortaleza de \textit{CyberOSS} (20B) es su madurez cognitiva, razonamiento lógico y resiliencia frente a la ambigüedad conceptual, siendo su única debilidad el alto tiempo de espera en inferencia debido al volumen de su arquitectura. Desde una perspectiva operativa, se recomienda delegar en \textit{Lily} la clasificación preliminar de alertas, el filtrado automatizado de observables y la generación de resúmenes bajo directrices muy específicas, mientras que el uso de \textit{CyberOSS} debe reservarse para el análisis forense profundo, la resolución de grafos de atribución de amenazas complejas y la toma de decisiones estratégicas donde la precisión técnica prevalezca sobre la inmediatez de la respuesta.


% Así se sustituye el número automáticamente:
% Como se ve en la Tabla~\ref{tab:dominio}
\newpage
\section{Conclusiones y trabajo futuro}
El desarrollo de este proyecto ha permitido diseñar e implementar una arquitectura de asistencia conversacional privada y \textit{on-premise} orientada al análisis de Indicadores de Compromiso (IoCs). A través de la integración de \textit{OpenCTI}, \OpenWebUI{} y ChromaDB, se ha demostrado la viabilidad técnica de construir entornos de Inteligencia Artificial que garantizan la soberanía de los datos corporativos sensibles sin depender de servicios en la nube.

Desde la perspectiva del diseño, el flujo de trabajo modular propuesto ha probado su eficiencia al resolver el desacoplamiento de datos mediante un backend RAG integrado. En esta arquitectura, la base de datos vectorial actúa como un componente de apoyo complementario que trabaja de forma conjunta con la API de \textit{OpenCTI}; mientras esta última suministra la ciberinteligencia relacional y estructurada, el almacén vectorial asiste al sistema recuperando contexto analítico adyacente bajo demanda. Esta cooperación mitiga las limitaciones de ventana de contexto de los modelos locales sin saturar estáticamente los prompts, confirmando la viabilidad de desplegar una plataforma de ciberinteligencia cohesiva utilizando herramientas de código abierto basadas en el estándar STIX.

En el plano cualitativo, las pruebas empíricas evidencian que el rendimiento del sistema está fuertemente ligado tanto a la escala de parámetros del modelo como a la precisión de las instrucciones suministradas. Mientras que \textit{Lily-Cybersecurity-7B-v0.2} ofrece una agilidad de respuesta óptima para entornos limitados a CPU gracias a su ligereza, presenta debilidades al procesar consultas abstractas. Por el contrario, \textit{CyberOSS-2.0-20B} demuestra una madurez cognitiva superior al discriminar el ruido informativo y contextualizar la incertidumbre, asumiendo un mayor coste de tiempo debido a la inferencia puramente basada en hardware de procesamiento. Asimismo, los resultados confirman que los modelos de menor escala incrementan notablemente su precisión cuando se les guía mediante tareas y comandos concretos frente a peticiones generales. A nivel cuantitativo, las desviaciones típicas extremadamente bajas de ambas arquitecturas constatan la estabilidad y previsibilidad de los motores de inferencia local sobre la CPU.

A partir de las conclusiones extraídas, se definen las siguientes líneas estratégicas de trabajo futuro para la evolución de la plataforma:

En primer lugar, se plantea optimizar la búsqueda semántica en ChromaDB y moderar el contexto entregado a los modelos. Es prioritario desarrollar una capa intermedia que filtre y reorganice la información recuperada, incorporando técnicas de compresión de contexto, algoritmos de \textit{reranking} y filtros condicionados por tipo de observable.

En segundo lugar, se contempla refinar la lógica de recuperación expandiendo el sistema hacia un asistente CTI general que explote de forma nativa las relaciones de \textit{OpenCTI}. Ello permitirá estructurar grafos relacionales profundos para formular análisis de campañas o perfiles de \textit{threat actors} basados en conocimiento formal. De manera complementaria, se implementará un preprocesamiento mediante un LLM intermedio que clasifique la intención de la consulta antes de la generación final, habilitando el soporte para múltiples IoCs en una sola frase o la identificación de datos ofuscados.

Finalmente, en el plano del despliegue, se avanzará hacia una infraestructura contenerizada completa mediante \textit{Docker Compose}, incorporando mecanismos automatizados para actualizar la base vectorial de forma incremental sin necesidad de regenerar el índice completo por lotes. Asimismo, se añadirán herramientas de logging estructurado para registrar la trazabilidad de los prompts e inyectar un sistema heurístico de citas en la respuesta final del asistente, forzando a los modelos a indicar si una afirmación procede de la ciberinteligencia factual de \textit{OpenCTI} o del almacenamiento semántico de ChromaDB.

\newpage
\addcontentsline{toc}{section}{Anexos}
\section*{Anexos}
\addcontentsline{toc}{subsection}{Anexo A. Organización del trabajo.}
\subsection*{Anexo A: Organización del trabajo.}
\begin{figure}[h!]
    \centering
    \includegraphics[width=0.95\linewidth]{Imagenes/gantt}
    \caption{Diagrama de Gantt}
    \label{fig:gantt}
\end{figure}
\begin{table}[h!]
	\centering
	\begin{tabular}{l r}
		\hline
		\textbf{Tarea} & \textbf{Horas } \\
		\hline
		 Estudio de montajes de IA locales &  10 \\
		  \hline
		 Estudio requisitos e implementación de infraestructura &  10 \\
		  \hline
		 Puesta en marcha interfaz web de chat & 20  \\
		  \hline
		 Desarrollo ingestador e ingesta de datos &  25 \\
		  \hline
		 Desarrollo backend y motor RAG &  55 \\
		  \hline
		 Integración interfaz, modelo y backend & 70  \\
		  \hline
		 Estudio de segundo modelo &  5 \\
		  \hline
		 Implementación segundo modelo & 5  \\
		  \hline
		 Comparativas y pruebas &  25 \\
		  \hline
		 Desarrollo memoria &   \\
		  \hline
		 Total &  225 \\
		 \hline
	\end{tabular}
	\caption{Tabla de tareas y tiempos}
	\label{tab:tareas}
\end{table}

\addcontentsline{toc}{subsection}{Anexo B. Ficheros desarrollados.}
\subsection*{Anexo B: Ficheros desarrollados.}
\label{sec:anexoB}

\lstinputlisting[language=Python,inputencoding=utf8/latin1,
breaklines=true,caption=client.py,label=lst:client]{python/client.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=ingest.py,label=lst:ingest]{python/ingest.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=normalizer.py,label=lst:normalizer]{python/normalizer.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=normalize\_all.py,label=lst:normalizeall]{python/normalize_all.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=embed\_all\_st.py,label=lst:embedall]{python/embed_all_st.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=chroma\_load.py,label=lst:chromaload]{python/chroma_load.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=docker-compose.yml,label=lst:dockercompose]{docker-compose.yml}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=lily\_pipeline.py,label=lst:lilypipeline]{python/lily_pipeline.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1, breaklines=true,caption=cybeross\_pipeline.py,label=lst:lcyberosspipeline]{python/cybeross_pipeline.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1,
breaklines=true,caption=app.py,label=lst:app]{python/app.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1,
breaklines=true,caption=ioc\_resolver.py,label=lst:iocresolver]{python/ioc_resolver.py}

\lstinputlisting[language=Python,inputencoding=utf8/latin1,
breaklines=true,caption=rag\_client.py,label=lst:ragclient]{python/rag_client.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1,
breaklines=true,caption=ioc\_context\_builder.py,label=lst:iocctxbuild]{python/ioc_context_builder.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1,
breaklines=true,caption=context\_profiles.py,label=lst:contextprofiles]{python/context_profiles.py}

\lstinputlisting[language=Python, inputencoding=utf8/latin1,
breaklines=true,caption=builder.py,label=lst:builder]{python/builder.py}


\addcontentsline{toc}{subsection}{Anexo C. Evaluación de modelos}
\subsection*{Anexo C. Evaluación de modelos}
\label{sec:anexoC}

Este anexo recoge los detalles de la evaluación realizada, incluyendo las preguntas que se han realizado a los modelos, cuáles han sido sus respuestas y las valoraciones que se les ha dado.

\subsubsection*{Criterios de evaluación}

\begin{table}[H]
	\centering
	\small
	\begin{tabularx}{\textwidth}{||X|c|c|C||} % <-- Usamos 'C' en la última columna
		\hline
		\textbf{Criterio} & \textbf{0} & \textbf{1} & \textbf{2} \\
		\hline
		¿Menciona indicadores explícitos? & No & Parcialmente & Completamente \\
		\hline
		¿Muestra bien el estado y \textit{confidence}? & No & Incompleto & Correcto \\
		\hline
		¿Menciona y separa el contexto inferido? & Ignora & Menciona pero mal & Menciona y separa correctamente \\
		\hline
		¿Separa relaciones fuertes y débiles? & Mezcla/confunde & Separa vagamente & Separa correctamente \\
		\hline
		¿Alucina? & Sí, gravemente & Levemente & No alucina \\
		\hline
		¿Es útil para análisis? & Genérico & Superficial & Útil para analista \\
		\hline
	\end{tabularx}
	\caption{Rúbrica de evaluación utilizada}
	\label{tab:criterios-evaluacion}
\end{table}

\subsubsection*{Preguntas utilizadas}

\begin{table}[ht]
	\centering
	\small % Añadido para mantener la consistencia con la tabla anterior y que "respire" mejor
	\begin{tabularx}{\textwidth}{||l|C||} % <-- Usamos 'C' para que la última columna sea dinámica y centrada
		\hline
		\textbf{Tipo de pregunta} & \textbf{Pregunta} \\
		\hline
		Dominio conocido & Provide details about resisterma.com.br \\
		\hline
		IP conocida & Provide some details about 85.209.195.160 \\
		\hline
		Hash conocido & Provide some details about 684634f51cb07d40e58c88b51c62ff1c83a18b69 \\
		\hline
		Pregunta abierta & Summarize information about 62.211.188.248 \\
		\hline
		Contexto adicional & What additional CTI context is available for officeonlaine.com? \\
		\hline
		Relación con actores & is 62.211.188.248 attributed to any actor according to context? \\
		\hline
		Separación fuerte/débil & Which information about officeonlaine.com is confirmed in OpenCTI and which is only inferred? \\
		\hline
	\end{tabularx}
	\caption{Preguntas utilizadas en la evaluación}
	\label{tab:preguntas-evaluacion}
\end{table}

\subsubsection*{Respuestas a las preguntas}
\textbf{Dominio conocido}\\
\textbf{Respuesta de Lily}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50
		]
		
		Based on the context provided, \texttt{resisterma.com.br} is a domain name that was marked as revoked with a confidence of 100 by the OpenCTI platform. This indicates that there may be some concern regarding its security or legitimacy.
		
		According to the context, this domain has two explicit indicators associated with it: \texttt{resisterma.com.br} and \url{http://resisterma.com.br/RIC/RCH.exe}. Both of these indicators have a marking of TLP:CLEAR, which suggests that they can be openly discussed within the cybersecurity community without compromising sensitive information.
		
		In terms of the explicit indicators, \texttt{resisterma.com.br} is marked as revoked and has a score of 20, while \url{http://resisterma.com.br/RIC/RCH.exe} is also marked as revoked but has a higher score of 30. It is important to note that these scores can provide some indication of the severity or risk associated with these indicators.
		
		The context also indicates that there are external references available for both indicators, including an external analysis link for \texttt{resisterma.com.br} and two separate external analysis links for \url{http://resisterma.com.br/RIC/RCH.exe}. These references may contain additional information or insights about these indicators that could be valuable for further investigation.
		
		It is worth noting that there is no explicit description provided for the threat actor associated with this domain in the context, but it does indicate that the domain was created on \texttt{2026-02-18T18:14:10.980Z}. The context also mentions another threat actor named \texttt{incransom} that may be related to the domain.
		
		Overall, the context suggests that there is some level of concern regarding the security or legitimacy of \texttt{resisterma.com.br} and associated indicators. Further investigation using the available external references could provide additional insights into this matter.
		
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 1/2 - Menciona el dominio y la URL asociada, pero no enumera todos los campos relevantes.\\
\textbf{¿Muestra bien el estado y confidence?} 1/2 - Indica correctamente que el dominio está revoked con confidence 100, y que la URL también está revoked pero en esta no menciona la confidence.\\
\textbf{¿Menciona y separa el contexto inferido?} 0/2 - No, ignora el contexto inferido.\\
\textbf{¿Separa relaciones fuertes y débiles?} 1/2 - Menciona que es información obtenida de OpenCTI y dice que falta contexto pero no separa explícitamente fuerte de débil, además de ignorar lo inferido.\\
\textbf{¿Alucina?} 1/2 - No gravemente, pero incluye información y muletillas no relacionadas con la pregunta, como "protect ourselves from malicious activities" o habla de inferencias no soportadas como "malware associated to this IOC".\\
\textbf{¿Es útil para análisis?} 1/2 - Superficialmente, provee de scoring y confidence básicos, pero no dice nada significativo.\\
\textbf{Total:} 5/12\\

\vspace{0.5cm}
\textbf{Respuesta de CyberOSS}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50,
		fonttitle=\bfseries
		]
		
		\subsubsection*{1. Explicit Indicators and Facts Extracted}
		
		\paragraph*{Domain Indicator}
		\begin{description}
			\item[Value:] \texttt{resisterma.com.br}
			\item[Type:] Domain name
			\item[Status:] Revoked
			\item[Confidence:] 100\%
			\item[Score:] 20
			\item[Detection:] False
			\item[Pattern:] \texttt{domain-name:value = 'resisterma.com.br'}
			\item[Valid From:] 2026-05-09 04:49:38.000 Z
			\item[Valid Until:] 2026-06-08 01:35:38.364 Z
			\item[Source Organization:] camichel.com
			\item[Marking:] TLP:CLEAR
			\item[External References:] \hfill
			\begin{itemize}
				\item \url{https://iris-h.services/#/pages/report/ae8e93f132a92370feee62db694895653f4aade2}
				\item \url{https://cape.contextis.com/submit/status/84215/}
				\item \url{https://malshare.com/sample.php?action=detail&hash=7f027a7e20cda803e49053eb1a518522}
			\end{itemize}
		\end{description}
		
		\paragraph*{URL Indicator}
		\begin{description}
			\item[Value:] \url{http://resisterma.com.br/RIC/RCH.exe}
			\item[Type:] URL
			\item[Status:] Revoked
			\item[Confidence:] 100\%
			\item[Score:] 30
			\item[Detection:] True
			\item[Pattern:] \texttt{url:value = 'http://resisterma.com.br/RIC/RCH.exe'}
			\item[Description:] binary
			\item[Valid From:] 2019-06-30 09:25:47.000 Z
			\item[Valid Until:] 2019-08-16 09:25:47.000 Z
			\item[Source Organization:] camichel.com
			\item[Marking:] TLP:CLEAR
			\item[External References:] \hfill
			\begin{itemize}
				\item \url{https://iris-h.services/#/pages/report/ae8e93f132a92370feee62db694895653f4aade2}
				\item \url{https://cape.contextis.com/submit/status/84215/}
				\item \url{https://malshare.com/sample.php?action=detail&hash=7f027a7e20cda803e49053eb1a518522}
			\end{itemize}
		\end{description}
		
		\subsubsection*{2. CTI Entities Described}
		
		\paragraph*{Threat Actors}
		\begin{description}
			\item[thegentlemen] -- No description or TTPs provided.
			\item[incransom] -- No description or TTPs provided.
			\item[anubis] -- No description or TTPs provided.
			\item[lapsus\$] -- No description or TTPs provided.
			\item[interlock] -- No description or TTPs provided.
		\end{description}
		
		\paragraph*{Malware}
		\begin{description}
			\item[Kazuar] -- A .NET-based backdoor with API exposure; linked to the Turla (Uroburos/Snake) group.
			\item[Bazar] -- A downloader/backdoor used since 2020, associated with TrickBot campaigns and capable of deploying additional payloads.
			\item[Volgmer] -- A backdoor Trojan used by Hidden Cobra, typically delivered via spear-phishing.
		\end{description}
		
		\paragraph*{Campaigns / Reports}
		No specific campaign names are listed; the context provides creation timestamps for threat-actor and malware entries but no campaign identifiers.
		
		\paragraph*{Techniques / TTPs}
		No explicit TTPs are listed for the threat actors; generic categories such as ``Trusted Relationship'' and ``Botnet'' appear under Bazar's description.
		
		\paragraph*{Infrastructure}
		The domain \texttt{resisterma.com.br} and the URL \url{http://resisterma.com.br/RIC/RCH.exe} are the primary infrastructure indicators. Both are marked as revoked and have defined validity periods.
		
		\subsubsection*{3. Relationships and Uncertainties}
		
		\paragraph*{Clear Relationships}
		\begin{itemize}
			\item The domain and URL are directly linked to the same threat-actor (camichel.com) and share identical external references, indicating they belong to a single malicious campaign.
			\item The URL points to a binary (\texttt{RCH.exe}), suggesting the domain was used to host or deliver malicious payloads.
		\end{itemize}
		
		\paragraph*{Uncertain / Missing Relationships}
		\begin{itemize}
			\item No explicit link between \texttt{resisterma.com.br} and any of the listed threat actors (\texttt{thegentlemen}, \texttt{incransom}, etc.) is provided.
			\item The malware families Kazuar, Bazar, and Volgmer are described in the broader context but there is no direct evidence tying them to \texttt{resisterma.com.br}.
			\item Campaign identifiers, specific TTPs, or operational details for the threat actors remain absent, leaving their connection to the domain speculative.
		\end{itemize}
		
		\paragraph*{Inferred Contextual Information}
		The similarity-search entries suggest that \texttt{resisterma.com.br} may be part of a larger set of indicators associated with the unnamed threat actors, but this is not explicitly confirmed.
		
		\subsubsection*{4. Summary of Findings}
		\begin{description}
			\item[Explicit Indicators:] The domain \texttt{resisterma.com.br} and the URL \url{http://resisterma.com.br/RIC/RCH.exe} are revoked, high-confidence indicators with defined validity windows and shared external analysis links.
			\item[CTI Entities:] Multiple threat actors and malware families are listed in the context, but only the domain and URL are directly observable.
			\item[Relationships:] The domain and URL are clearly connected; however, their association with specific threat actors or malware families is not explicitly documented, leaving those links uncertain.
		\end{description}
		
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona completamente los dos indicadores explícitos principales: el dominio \texttt{resisterma.com.br} y la URL correspondiente \texttt{http://resisterma.com.br/RIC/RCH.exe}, incluyendo sus tipos, patrones, fuentes, periodos de validez y referencias externas.\\
\textbf{¿Muestra bien el estado y confidence?} 2/2 - Muestra correctamente que resisterma.com.br está Active con Confidence 100, Score 30 y Detection True, y que la URL http://resisterma.com.br/RIC/RCH.exe está Revoked con Confidence 100, Score 30 y Detection True.\\
\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona correctamente los threat actors inferidos thegentlemen e incransom, indica que proceden de la sección “Contextual CTI Information (Inferred from Similarity)” y no los presenta como atribución confirmada.\\
\textbf{¿Separa relaciones fuertes y débiles?} 2/2 - Separa bien las relaciones explícitas de las relaciones inferidas: identifica la relación técnica directa entre el dominio y la URL porque la URL cuelga del dominio, y aclara que los threat actors son solo contexto por similitud sin evidencia directa de relación.\\
\textbf{¿Alucina?} 1/2 - No inventa una atribución grave, pero contiene alguna imprecisión: afirma en el resumen que la URL está vinculada al mismo “timeframe” que el dominio, cuando sus periodos de validez son distintos (dominio en 2026 y URL en 2019), y también usa “share identical metadata” de forma algo amplia, aunque sí comparten confidence, score, source organization y marking.\\
\textbf{¿Es útil para análisis?} 2/2 - Es muy útil para análisis porque estructura claramente los indicadores explícitos, resume sus atributos relevantes, identifica la relación técnica dominio-URL, menciona el contexto inferido y marca correctamente las incertidumbres sobre los threat actors.\\
\textbf{Total:} 11/12\\


\textbf{IP conocida}\\
\textbf{Respuesta de Lily}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50
		]
		
		Based on the context provided, \texttt{85.209.195.160} is an IP address that has been identified as aggressive and known to be malicious based on its listing in the AbuseIPDB database. The country code associated with this IP address is DE, indicating it is located in Germany. According to the last reported data, this IP address was first reported at \texttt{2026-05-19T08:00:55+00:00}.
		
		Furthermore, the information retrieved via semantic similarity search reveals that there are two threat actors named \texttt{thegentlemen} and \texttt{incransom}, but no detailed descriptions are available for these entities. It is important to note that this contextual CTI information is inferred through semantic similarity, meaning it may not be entirely accurate or reliable.
		
		In summary, \texttt{85.209.195.160} is an aggressive and malicious IP address located in Germany, as indicated by its listing in the AbuseIPDB database. While there are also two threat actors named \texttt{thegentlemen} and \texttt{incransom}, their descriptions are not explicitly provided in the context.
		
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 1/2 - El indicador principal es mencionado e identificado como una IP maliciosa de la fuente AbuseIPDB, pero no habla de los factores presentes en el contexto.\\
\textbf{¿Muestra bien el estado y confidence?} 1/2 - Menciona que tiene una abuse score de 100, pero no menciona la confidence y status de OpenCTI.\\
\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Sí, los actores adicionales son mencionados como info adicional.\\
\textbf{¿Separa relaciones fuertes y débiles?} 1/2 - Separa parcialmente, trata la IP como explícita y los actores adicionales como información extra, aunque no menciona que lo adicional no está directamente relacionado con el indicador, sino que es inferencia.\\
\textbf{¿Alucina?} 1/2 - No gravemente, pero sí hace sugerencias que no se le piden.\\
\textbf{¿Es útil para análisis?} 1/2 - Superficialmente ya que provee una síntesis básica pero carece de un desglose con métricas avanzadas.\\
\textbf{Total:} 7/12\\

\textbf{Respuesta de CyberOSS}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50,
		fonttitle=\bfseries
		]
		\shorthandoff{"} % Desactivación preventiva para textos en inglés del modelo
		
		\subsubsection*{1. Explicit Indicators and Facts Extracted}
		
		\begin{description}
			\item[IPv4 Address:] \texttt{85.209.195.160}
			\item[Status:] Revoked (indicating the IP is no longer active or has been taken down)
			\item[Confidence Level:] 100\% (high confidence that the IP is malicious)
			\item[Score:] 20 (moderate risk rating)
			\item[Detection:] True (the IP has been detected as malicious)
			\item[Pattern:] \texttt{ipv4-addr:value = '85.209.195.160'}
			\item[Description:] Identified as an aggressive IP known to be malicious on AbuseIPDB, with a country code of \textbf{DE} (Germany) and an abuse confidence score of \textbf{100}. Last reported at \textbf{2026-05-19T08:00:55+00:00}.
			\item[Validity Period:] Valid from \textbf{2026-05-22T02:17:54.000Z} to \textbf{2026-06-11T07:52:33.434Z}.
			\item[Source Organization:] AbuseIPDB.
			\item[Marking:] TLP:CLEAR (information can be shared freely).
			\item[External References:] \hfill
			\begin{itemize}
				\item AbuseIPDB database URL: \url{https://www.abuseipdb.com/}
				\item MISP event URL: \url{https://misp.reyes.ccn-cert.cni.es/events/view/0cc6bb85-748b-4834-bf85-9f0bd8a5eab4}
			\end{itemize}
		\end{description}
		
		\subsubsection*{2. CTI Entities Mentioned}
		
		\begin{description}
			\item[Threat Actors:] None explicitly linked to this IP in the provided data.
			\item[Reports:] No specific reports are cited beyond the AbuseIPDB entry.
			\item[Malware:] No malware is directly associated with this IP in the provided context.
			\item[Campaigns:] No campaigns are mentioned in connection with this IP.
			\item[Techniques (TTPs)] No specific TTPs are listed for this IP.
			\item[Infrastructure:] The IP itself is the infrastructure element under analysis.
		\end{description}
		
		\subsubsection*{3. Relationships and Uncertainties}
		
		\phantomsection % Asegura el destino hipertextual para evitar warnings en el PDF
		\paragraph*{Explicit Relationships}
		
		\begin{itemize}
			\item The IP is explicitly linked to AbuseIPDB as a known malicious address with high confidence.
			\item The validity period and detection status are clearly defined.
		\end{itemize}
		
		\phantomsection % Asegura el destino hipertextual para evitar warnings en el PDF
		\paragraph*{Uncertainties}
		
		\begin{itemize}
			\item \textbf{Threat Actor Association:} No direct threat actor is associated with this IP in the provided data.
			\item \textbf{Malware or Campaign Linkage:} There is no explicit connection to specific malware or campaigns.
			\item \textbf{TTPs:} No specific techniques are mentioned for this IP.
			\item \textbf{Broader Context:} While the IP is marked as aggressive and malicious, the lack of additional context, such as associated threat actors, malware, or campaigns, limits the ability to place it within a broader threat landscape.
		\end{itemize}
		
		\subsubsection*{4. Summary}
		
		The IP address \textbf{\texttt{85.209.195.160}} is a high-confidence malicious indicator from AbuseIPDB, valid between May and June 2026. It is marked as revoked, indicating it is no longer active. No direct links to threat actors, malware, or campaigns are provided in the context, leaving its broader threat role unclear. Further investigation would be required to determine any additional associations or operational use cases.
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
\large{\textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente el indicador explícito principal, \texttt{85.209.195.160}, identificándolo como una dirección IPv4 e incluyendo patrón, descripción AbuseIPDB, fuente, marking, referencias externas y periodo de validez.\\
\textbf{¿Muestra bien el estado y confidence?} 2/2 - Muestra correctamente que el indicador está \texttt{Revoked}, con \texttt{Confidence: 100}, \texttt{Score: 20} y \texttt{Detection: True}. También recoge el país \texttt{DE}, el \texttt{abuseConfidenceScore: 100} y la fecha \texttt{lastReportedAt}.\\
\textbf{¿Menciona y separa el contexto inferido?} 1/2 - No enumera todos los objetos recuperados por similitud semántica, como \texttt{thegentlemen}, \texttt{incransom}, \texttt{anubis}, \texttt{tengu}, \texttt{osiris}, \texttt{NanHaiShu}, \texttt{PlugX} o \texttt{Kazuar}. Sin embargo, sí deja claro que no hay actores, malware, campañas o técnicas directamente asociados al IOC, por lo que reconoce parcialmente que cualquier contexto adicional no constituye una relación confirmada.\\
\textbf{¿Separa relaciones fuertes y débiles?} 1/2 - Separa correctamente la relación fuerte entre la IP y AbuseIPDB/MISP como fuente explícita del indicador, pero no desarrolla suficientemente la parte débil o inferida. Debería haber indicado que los actores y malware recuperados por similitud aparecen solo como contexto semántico no confirmado.\\
\textbf{¿Alucina?} 2/2 - No inventa atribuciones a actores, malware o campañas, y se mantiene prudente al afirmar que no hay relaciones explícitas adicionales. La interpretación de \texttt{Revoked} como indicador revocado se considera aceptable dentro del contexto de OpenCTI y no supone una alucinación relevante.\\
\textbf{¿Es útil para análisis?} 1/2 - Es útil parcialmente porque resume bien la información explícita del indicador, incluyendo AbuseIPDB, confidence, estado, score, detection, fechas y referencias. No obstante, queda incompleta para análisis CTI porque no aprovecha el contexto inferido disponible ni enumera los objetos recuperados por similitud.\\
\textbf{Total:} 9/12.\\


\textbf{Hash conocido}

\textbf{Respuesta de Lily}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50
		]
		
		Based on the provided context, here are some details about the indicator \texttt{684634f51cb07d40e58c88b51c62ff1c83a18b69}:
		
		\begin{itemize}
			\item The indicator is represented by a unique identifier.
			\item It has one direct pattern indicator and one pattern-based indicator associated with it.
			\item There is only one unique indicator shown for this IOC according to OpenCTI.
			\item The indicator is related to the following threat actors:
			\begin{itemize}
				\item \textbf{THREAT\_ACTOR:} \texttt{thegentlemen}, which was created on \texttt{2026-02-18T18:14:10.980Z} and has no description available.
				\item \textbf{THREAT\_ACTOR:} \texttt{incransom}, which was created on \texttt{2026-02-18T18:14:08.911Z} and also has no description available.
			\end{itemize}
			\item No further details about the indicator or its relationship to these threat actors are provided in the context. However, we can infer that it is associated with cybersecurity threats based on the presence of the indicators and the mention of the threat actors.
		\end{itemize}
		
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente el hash y el indicador explícito \texttt{URGENT\_QUOTATION\_PR \#270473\_07\_10\_2020.exe} y su hash correspondiente \texttt{684634f51cb07d40e58c88b51c62ff1c83a18b69}.\\
\textbf{¿Muestra bien el estado y confidence?} 2/2 - Indica correctamente el estado Revoked, la confidence 100, el score 30 y Detection: True.\\
\textbf{¿Menciona y separa el contexto inferido?} 0/2 - Ignora los threat actors inferidos thegentlemen e incransom y además afirma que no puede determinar información inferida adicional, aunque sí existe una sección de contexto inferido.\\
\textbf{¿Separa relaciones fuertes y débiles?} 1/2 - Separa razonablemente la información explícita, pero no trata la parte inferida; debería haber indicado que thegentlemen e incransom son contexto por similitud y no atribución confirmada.\\
\textbf{¿Alucina?} 2/2 - No inventa relaciones graves; la única imprecisión menor es decir “created on” para un campo que realmente aparece como Valid from, pero no supone una alucinación grave.\\
\textbf{¿Es útil para análisis?} 1/2 - Es útil parcialmente porque identifica el hash, el fichero asociado, el estado, la confianza, la detección, la descripción, la organización fuente y el marcado, pero queda incompleta al no mencionar los actores inferidos, no usar la referencia externa MISP de campaña y no formular una conclusión clara sobre qué está confirmado y qué no.\\
\textbf{Total:} 8/12\\

\textbf{Respuesta de CyberOSS}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50,
		fonttitle=\bfseries
		]
		
		\subsubsection*{1. Extracted explicit indicators and facts}
		
		% Eliminados parámetros de extensión conflictivos; dejamos el entorno nativo limpio
		\begin{description}
			\item[File hash (SHA-1):] \texttt{684634f51cb07d40e58c88b51c62ff1c83a18b69}
			\item[File name:] \texttt{URGENT\_QUOTATION\_PR \#270473\_07\_10\_2020.exe}
			\item[File type / MIME:] \texttt{application/x-dosexec} (Windows executable)
			\item[Hash type:] SHA-1
			\item[StixFile status:] \textbf{Revoked} (confidence = 100\%)
			\item[Score:] 30
			\item[Detection:] True
			\item[Pattern:] \texttt{file:name = 'URGENT\_QUOTATION\_PR \#270473\_07\_10\_2020.exe' AND file:hashes.'SHA-1' = '684634f51cb07d40e58c88b51c62ff1c83a18b69'}
			\item[Archive contents:] The executable is contained in an archive whose source file is \texttt{URGENT\_QUOTATION\_PR \#270473\_07\_10\_2020.pdf.Gz}.
			\item[Valid from:] 2020-10-14 07:58:31 UTC
			\item[Valid until:] 2022-01-17 07:58:31 UTC
			\item[Source organization:] CERT PKO BP
			\item[Marking:] TLP:CLEAR
			\item[External reference:] \url{https://misp.reyes.ccn-cert.cni.es/events/view/08c2dca4-3544-4fdd-a1c7-53fa6dbfb318}
		\end{description}
		
		\subsubsection*{2. CTI entities mentioned in the context}
		
		\renewcommand{\arraystretch}{1.25}
		% Se eliminan las barras verticales dobles '||' para garantizar compatibilidad con booktabs
		\begin{tabularx}{\textwidth}{>{\bfseries}p{2.8cm} p{4.2cm} X}
			\toprule
			Entity type & Identifier / Name & Description from the context \\
			\midrule
			StixFile & \texttt{684634f51cb07d40e58c88b51c62ff1c83a18b69} & The file described above; status = revoked. \\
			Threat actors & \texttt{thegentlemen}, \texttt{incransom}, \texttt{anubis}, \texttt{tengu}, \texttt{bravox} & Listed in the similarity-based CTI section; no further details provided. \\
			Malware & \texttt{NanHaiShu}, \texttt{PlugX}, \texttt{DeedRAT} & Descriptions and TTPs are given in the similarity-based section. \\
			Campaign & MISP campaign URL & Associated with the file indicator. \\
			TTPs & Various techniques & Listed under the malware entries in the similarity-based section. \\
			\bottomrule
		\end{tabularx}
		
		\subsubsection*{3. Relationships that are clearly present}
		
		\begin{itemize}
			\item The \textbf{StixFile} \texttt{684634f51cb07d40e58c88b51c62ff1c83a18b69} is directly linked to the \textbf{MISP campaign} URL in the external references.
			\item The file's \textbf{name} and \textbf{SHA-1 hash} are the exact pattern used to identify the indicator.
			\item The \textbf{validity period} defines the time window during which the indicator was considered active.
			\item The \textbf{source organization} is CERT PKO BP and the marking is \textbf{TLP:CLEAR}.
		\end{itemize}
		
		\subsubsection*{4. Relationships that remain uncertain}
		
		\begin{itemize}
			\item The similarity-based CTI section lists several threat actors and malware families, but the context does not state any explicit link between these entities and the file hash.
			\item No explicit technique is tied to the file itself; the TTPs belong to the malware entries in the similarity-based section.
		\end{itemize}
		
		\subsubsection*{5. Summary of the analysis}
		
		The hash \texttt{684634f51cb07d40e58c88b51c62ff1c83a18b69} identifies a Windows executable named \texttt{URGENT\_QUOTATION\_PR \#270473\_07\_10\_2020.exe}. The indicator is a \textbf{revoked StixFile} published by \textbf{CERT PKO BP}, valid from \textbf{14 Oct 2020} to \textbf{17 Jan 2022}, and is associated with a \textbf{MISP campaign.}
		
		The broader CTI context lists several threat actors and malware families, but none are explicitly connected to this file. Consequently, the only concrete relationships are those defined by the indicator's metadata.
		
	\end{tcolorbox}
\end{small}
\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona completamente el indicador explícito asociado al hash: URGENT\_QUOTATION\_PR \#270473\_07\_10\_2020.exe 684634f51cb07d40e58c88b51c62ff1c83a18b69, identifica el fichero, el hash SHA-1 y recoge los principales campos del indicador.\\
\textbf{¿Muestra bien el estado y confidence?} 2/2 - Muestra correctamente Status: Revoked, Confidence: 100, Score: 30 y Detection: True, además del tipo StixFile, el patrón, el periodo de validez, la organización fuente CERT PKO BP y el marking TLP:CLEAR.\\
\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona correctamente los threat actors inferidos thegentlemen e incransom, indica que proceden de similitud semántica y no los presenta como relaciones confirmadas con el hash o el fichero.\\
\textbf{¿Separa relaciones fuertes y débiles?} 2/2 - Separa adecuadamente la relación fuerte/confirmada entre el hash y el fichero indicado por OpenCTI de la relación débil/inferida con los threat actors, aclarando que no hay evidencia directa ni atribución confirmada.\\
\textbf{¿Alucina?} 1/2 - No inventa atribución grave y mantiene bien la incertidumbre, pero contiene alguna interpretación algo discutible: afirma que la revocación implica que el indicador fue “withdrawn or superseded”, cuando el contexto solo indica Status: Revoked sin explicar la causa; también dice que el fichero está vinculado a una campaña registrada en MISP, lo cual es razonable por la referencia externa, pero no debe interpretarse como una relación analítica más fuerte de lo que el contexto aporta.\\
\textbf{¿Es útil para análisis?} 2/2 - Es muy útil para análisis porque estructura claramente los indicadores explícitos, resume sus atributos relevantes, identifica la relación técnica dominio-URL, menciona el contexto inferido y marca correctamente las incertidumbres sobre los threat actors.\\
\textbf{Total:} 11/12.\\


\subsection*{Pregunta abierta}

\textbf{Respuesta de Lily}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50
		]
		\shorthandoff{"} % Desactivación preventiva para evitar el colapso de caracteres ingleses
		
		Based on the context provided, here is a summary of the information related to IP address \texttt{62.211.188.248}:
		
		\begin{description}
			\item[1.] The IP address \texttt{62.211.188.248} is explicitly mentioned in the context as an indicator related to this IOC.
			
			\item[2.] According to OpenCTI, this IP address is marked as revoked with a confidence of 100. It also has a detection score of 20 and is classified as an IPv4-Addr.
			
			\item[3.] The score for the pattern \texttt{[ipv4-addr:value = '62.211.188.248']} is 20, indicating that it has been detected with some degree of confidence.
			
			\item[4.] The indicator related to this IP address \texttt{62.211.188.248} is explicitly described as an aggressive IP known to be malicious on AbuseIPDB. It belongs to Italy and has a last reported date of \texttt{2026-05-09T20:05:50+00:00}.
			
			\item[5.] The validity period for this indicator is from \texttt{2026-05-05T09:38:12.488Z} to \texttt{2026-06-06T22:11:56.810Z}.
			
			\item[6.] The source organization responsible for this indicator is AbuseIPDB. The marker for the indicator is TLP:CLEAR.
			
			\item[7.] In the contextual CTI information, there are two threat actors mentioned without descriptions: \texttt{thegentlemen} and \texttt{incransom}. However, it is unclear whether these threat actors have any direct relationship or association with the IP address \texttt{62.211.188.248}.
		\end{description}
		
		Overall, this contextual information suggests that IP address \texttt{62.211.188.248} is known to be malicious and has been marked as such by AbuseIPDB. It falls under the category of an aggressive IP in Italy, with a last reported date in May 2026. The contextual CTI information mentions two threat actors, but their relationship or association with this IP address remains uncertain.
		
		\shorthandon{"} % Restauración segura de los shorthands en español
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente el indicador explícito 62.211.188.248 y lo identifica como una IP asociada a información confirmada de OpenCTI/AbuseIPDB.\\
\textbf{¿Muestra bien el estado y confidence?} 2/2 - Indica correctamente que el indicador está Active y que tiene confidence 100; también menciona el abuseConfidenceScore 100, Detection false, lastReportedAt y el periodo de validez.\\
\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona los threat actors inferidos thegentlemen e incransom y aclara que proceden de semantic similarity search/contextual CTI information.\\
\textbf{¿Separa relaciones fuertes y débiles?} 2/2 - Separa correctamente la relación explícita de la IP con AbuseIPDB/OpenCTI de la relación débil o no confirmada con los threat actors inferidos, indicando que no existe una relación explícita entre ellos y la IP.\\
\textbf{¿Alucina?} 1/2 - No inventa atribución grave, pero hay una inferencia algo imprecisa al decir que el indicador “matches a specific pattern or signature known to be associated with malicious activity”; el patrón mostrado es principalmente un patrón STIX de valor IPv4, no necesariamente una firma analítica más rica.\\
\textbf{¿Es útil para análisis?} 2/2 - Es útil para un analista porque resume el IOC, su estado, confianza, fuente AbuseIPDB, país, abuse score, fecha de último reporte, periodo de validez y contexto inferido, además de advertir correctamente que no hay atribución confirmada a los threat actors.\\
\textbf{Total:} 11/12.  Podemos ver que esta pregunta, muy parecida a la pregunta de IP conocida, pero diferente en la forma de enunciarse, pues la de IP conocida es "Provide details about", mientras que esta es "Summarize information". En la segunda claramente se le da una tarea al LLM, mientras que la primera es más vaga, ya que "dar detalles" es simplemente listar información para el LLM sin mucho que razonar (no tiene más detalles y datos que el contexto), mientras que "resumir información" le fuerza a procesar el contexto que se le pasa y generar una respuesta más rica.\\

\textbf{Respuesta de CyberOSS}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50,
		fonttitle=\bfseries
		]
		\shorthandoff{"} % Previene conflictos con las comillas dobles en inglés
		
		\subsubsection*{1. Explicit Indicators and Facts Extracted}
		
		\begin{description}
			\item[IPv4 address:] \texttt{62.211.188.248}
			\item[Status:] Revoked
			\item[Confidence level:] 100
			\item[Score:] 20
			\item[Detection:] False
			\item[Pattern:] \texttt{ipv4-addr:value = '62.211.188.248'}
			\item[Description:] Known malicious IP on AbuseIPDB; country code \textbf{IT}; abuse confidence score \textbf{100}; last reported at \textbf{2026-05-09T20:05:50+00:00}
			\item[Validity period:] \hfill
			\begin{itemize}
				\item \textbf{Valid from:} \texttt{2026-05-05T09:38:12.488Z}
				\item \textbf{Valid until:} \texttt{2026-06-06T22:11:56.810Z}
			\end{itemize}
			\item[Source organization:] AbuseIPDB
			\item[Marking:] TLP:CLEAR
			\item[External reference:] \url{https://www.abuseipdb.com/}
		\end{description}
		
		\subsubsection*{2. Described CTI entities}
		
		\phantomsection
		\paragraph*{a. Threat actors}
		
		\begin{description}
			\item[thegentlemen] -- No description available; created 2026-02-18T18:14:10.980Z
			\item[incransom] -- No description available; created 2026-02-18T18:14:08.911Z
			\item[anubis] -- No description available; created 2026-02-18T18:14:08.325Z
			\item[bravox] -- No description available; created 2026-02-18T18:14:12.147Z
			\item[lapsus\$] -- No description available; created 2026-03-01T10:54:04.617Z
		\end{description}
		
		\phantomsection
		\paragraph*{b. Malware}
		
		\begin{description}
			\item[NanHaiShu] -- Whitepaper identifies it as a malicious program targeting government and private-sector organizations; created 2026-01-16T11:51:31.631Z
			\item[PlugX] -- Malware with numerous TTPs: Msiexec, malicious file, drive-by compromise, spearphishing link, web protocols, malicious link, match legitimate name or location, dynamic API resolution, email accounts, DLL search order hijacking and PowerShell; created 2026-01-16T11:26:15.991Z
			\item[LP-Notes] -- No description available; created 2026-01-19T16:25:20.948Z
		\end{description}
		
		\phantomsection
		\paragraph*{c. Campaigns}
		
		No explicit campaign information is provided in the context.
		
		\phantomsection
		\paragraph*{d. Techniques (TTPs)}
		
		For \textbf{PlugX}, the following techniques are listed:
		
		\begin{itemize}
			\item Msiexec
			\item Malicious File
			\item Drive-by Compromise
			\item Spearphishing Link
			\item Web Protocols
			\item Malicious Link
			\item Match Legitimate Name or Location
			\item Dynamic API Resolution
			\item Email Accounts
			\item DLL Search Order Hijacking
			\item PowerShell
		\end{itemize}
		
		\subsubsection*{3. Explained relationships and uncertainties}
		
		\phantomsection
		\paragraph*{Explicitly related entities}
		
		\begin{itemize}
			\item The IP address \textbf{\texttt{62.211.188.248}} is directly linked to AbuseIPDB as a known malicious IP with confidence 100.
			\item The IP's status is ``Revoked'', indicating it was previously flagged as malicious but has since been cleared.
		\end{itemize}
		
		\phantomsection
		\paragraph*{Inferred relationships via semantic similarity}
		
		\begin{itemize}
			\item The list of threat actors (\texttt{thegentlemen}, \texttt{incransom}, \texttt{anubis}, \texttt{bravox}, \texttt{lapsus\$}) and malware (\texttt{NanHaiShu}, \texttt{PlugX}, \texttt{LP-Notes}) are presented in the same context, but no direct association with the IP address is stated.
			\item Therefore, while these entities are part of the broader CTI dataset, there is \textbf{no explicit evidence} that they used or are connected to \textbf{\texttt{62.211.188.248}}.
		\end{itemize}
		
		\phantomsection
		\paragraph*{Uncertainties}
		
		\begin{itemize}
			\item The context does not provide any campaign name or specific TTPs for the threat actors, leaving their operational details unknown.
			\item The only concrete link for the IP is its AbuseIPDB entry; any other connections would be speculative without additional data.
		\end{itemize}
		
		\subsubsection*{Summary}
		
		The IP \textbf{\texttt{62.211.188.248}} is a revoked, high-confidence malicious address recorded in AbuseIPDB, with country code IT and last reported on 2026-05-09. The surrounding CTI data lists several threat actors and malware families, but none are explicitly tied to this IP. Consequently, the only factual intelligence about the address is its AbuseIPDB status and associated metadata.
		
		\shorthandon{"} % Reactiva los shorthands de babel en español
	\end{tcolorbox}
\end{small}

\vspace{0.5cm}
{\large \textbf{Valoración}}\\
\normalsize
\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente el indicador explícito 62.211.188.248 e identifica sus campos principales: tipo IPv4, patrón STIX, descripción AbuseIPDB, fuente, periodo de validez, marking y referencia externa.\\
\textbf{¿Muestra bien el estado y confidence?} 2/2 - Muestra correctamente Status: Active, Confidence 100, Score 50, Detection False, abuseConfidenceScore 100, countryCode IT y lastReportedAt.\\
\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona correctamente los threat actors inferidos thegentlemen e incransom, indica que proceden de semantic similarity search y aclara que no están directamente vinculados a la IP en los datos proporcionados.\\
\textbf{¿Separa relaciones fuertes y débiles?} 2/2 - Separa adecuadamente la relación fuerte/confirmada entre la IP y AbuseIPDB de la relación débil/inferida con los threat actors, indicando que no hay evidencia directa ni enlace explícito entre thegentlemen/incransom y la IP.\\
\textbf{¿Alucina?} 1/2 - No inventa una atribución confirmada, pero sí introduce una inferencia no soportada al decir que la IP podría estar implicada en C2, distribución de malware u otras operaciones ilícitas; el contexto solo indica “aggressive IP known malicious on AbuseIPDB” y no especifica esa naturaleza concreta de actividad.\\
\textbf{¿Es útil para análisis?} 2/2 - Es útil para análisis porque resume de forma estructurada el IOC, sus atributos técnicos, fuente, validez, referencia externa, contexto inferido y limitaciones analíticas, además de evitar atribución directa no confirmada a actores.\\
\textbf{Total:} 11/12.\\


\subsection*{Contexto CTI adicional}

\textbf{Respuesta de Lily}
\begin{small}
	\begin{tcolorbox}[
		breakable,
		colback=gray!3,
		colframe=gray!50
		]
		
		Based on the context provided, there is limited additional CTI context available for \texttt{officeonlaine.com} beyond what has already been explicitly mentioned. However, it is worth noting that semantic similarity search revealed two threat actors associated with this IOC: \texttt{thegentlemen} and \texttt{incransom}. Their descriptions are not available in the context.
		
		In terms of techniques or infrastructure related to \texttt{officeonlaine.com}, there is no explicit indication in the provided context. It would be beneficial to gather more intelligence on these threat actors and potentially correlate them with other known campaigns or attack vectors to gain a better understanding of the security risks associated with \texttt{officeonlaine.com}.
		
		Overall, it is important for cybersecurity professionals like us to continue monitoring and analyzing CTI information to stay up-to-date on any potential threats and vulnerabilities related to \texttt{officeonlaine.com}.
		
		\end{tcolorbox}
		\end{small}
		
		\vspace{0.5cm}
		{\large \textbf{Valoración}}\\
		\normalsize
		\textbf{¿Menciona indicadores explícitos?} 1/2 - Menciona \texttt{officeonlaine.com}, pero no recoge correctamente los dos indicadores explícitos disponibles: el dominio \texttt{officeonlaine.com} y la URL \url{http://officeonlaine.com/up/update.exe}. Tampoco menciona atributos explícitos relevantes como patrón, fuente \texttt{iSight}, descripción \texttt{Red Kontos}, referencias MISP o periodo de validez.\\
		\textbf{¿Muestra bien el estado y confidence?} 0/2 - No menciona que ambos indicadores están \texttt{Revoked}, ni que tienen \texttt{Confidence: 100}, ni los scores \texttt{60} y \texttt{30}, ni \texttt{Detection: True}.\\
		\textbf{¿Menciona y separa el contexto inferido?} 1/2 - Menciona \texttt{thegentlemen} e \texttt{incransom}, pero ignora otros elementos inferidos presentes en el contexto, como \texttt{safepay}, \texttt{osiris}, \texttt{morpheus}, \texttt{PlugX}, \texttt{ZeroT} y \texttt{LP-Notes}. Además, dice que los actores están ``associated with this IOC'', lo cual es demasiado fuerte: el contexto solo indica que fueron recuperados por similitud semántica.\\
		\textbf{¿Separa relaciones fuertes y débiles?} 0/2 - No separa bien las relaciones explícitas de las inferidas. Presenta \texttt{thegentlemen} e \texttt{incransom} como asociados al IOC, cuando deberían tratarse como contexto inferido no confirmado. Tampoco distingue la relación fuerte de los indicadores OpenCTI frente al contexto semántico débil.\\
		\textbf{¿Alucina?} 1/2 - No inventa una atribución concreta grave, pero sí formula incorrectamente que \texttt{thegentlemen} e \texttt{incransom} están asociados con el IOC. También afirma que no hay técnicas o infraestructura relacionadas, cuando el contexto sí incluye malware y TTPs asociadas a \texttt{PlugX}, aunque no estén explícitamente vinculadas al dominio.\\
		\textbf{¿Es útil para análisis?} 1/2 - Es parcialmente útil porque identifica que hay contexto inferido y menciona dos actores, pero es superficial, omite gran parte del contexto disponible y termina con recomendaciones genéricas. No proporciona una separación clara entre lo confirmado por OpenCTI y lo inferido por similitud.\\
		\textbf{Total:} 4/12
		
		
		\textbf{Respuesta de CyberOSS}
		\begin{small}
			\begin{tcolorbox}[
				breakable,
				colback=gray!3,
				colframe=gray!50
				]
				\shorthandoff{"} % Previene conflictos con las comillas dobles en inglés o formatos de fecha
				
				\subsubsection*{1. Explicit Indicators and Facts Extracted}
				
				\phantomsection
				\paragraph*{Domain Indicator}
				
				\begin{description}
					\item[Value:] \texttt{officeonlaine.com}
					\item[Type:] Domain-Name
					\item[Status:] Revoked
					\item[Confidence:] 100\%
					\item[Score:] 60
					\item[Detection:] True
					\item[Pattern:] \texttt{[domain-name:value = 'officeonlaine.com']}
					\item[Description:] ``Red Kontos''
					\item[Validity:] 2025-01-16 T17:11:19 Z $\rightarrow$ 2025-11-12 T17:11:19 Z
					\item[Source Organization:] iSight
					\item[Marking:] TLP:CLEAR
					\item[External References:] \hfill
					\begin{itemize}
						\item MISP event ``ROBORAT Malware Targets Think Tanks, Human Rights, Diplomacy, and Hospitality; Suspected Kazakhstan Nexus''
						\item MISP event ``The Lowdown. A Candid Take on Cyber Operations (April 2018)''
					\end{itemize}
				\end{description}
				
				\phantomsection
				\paragraph*{URL Indicator}
				
				\begin{description}
					\item[Value:] \url{http://officeonlaine.com/up/update.exe}
					\item[Type:] URL
					\item[Status:] Revoked
					\item[Confidence:] 100\%
					\item[Score:] 30
					\item[Detection:] True
					\item[Pattern:] \texttt{[url:value = 'http://officeonlaine.com/up/update.exe']}
					\item[Validity:] 2025-01-16 T16:54:49 Z $\rightarrow$ 2025-03-04 T16:54:49 Z
					\item[Source Organization:] iSight
					\item[Marking:] TLP:CLEAR
					\item[External Reference:] Same MISP event as above (ROBORAT).
				\end{description}
				
				\subsubsection*{2. CTI Entities Mentioned}
				
				\renewcommand{\arraystretch}{1.25}
				\begin{tabularx}{\textwidth}{>{\bfseries}p{3cm} p{3cm} X X}
					\toprule
					Entity & Type & Description / Role & Relationship to \texttt{officeonlaine.com} \\
					\midrule
					iSight & Source Organization & Provides the indicator data (domain and URL) for \texttt{officeonlaine.com}. & Direct source of the IOC data. \\
					
					ROBORAT Malware & Malware & Targeted think-tanks, human-rights groups, diplomacy and hospitality sectors; linked to a suspected Kazakhstan nexus. & The domain \texttt{officeonlaine.com} is cited in the MISP event describing ROBORAT activity, suggesting the domain may have been used by or associated with this malware family. \\
					
					The Lowdown & Report & A MISP-published analysis of cyber operations (April 2018). & The domain appears in this report, indicating it was observed in broader cyber-operation discussions. \\
					
					PlugX & Malware & Remote-access Trojan (RAT) delivered via various droppers. & Listed in the contextual CTI as a malware family; no explicit link to \texttt{officeonlaine.com} but appears in the same semantic-search context. \\
					
					ZeroT & Malware & Downloader used to install PlugX; observed targeting military and aerospace interests in Russia/Belarus. & Also appears in the contextual CTI; no direct link to the domain. \\
					
					LP-Notes & Malware & No description provided. & Appears in the contextual CTI; no direct link to the domain. \\
					
					Threat Actors: \texttt{thegentlemen}, \texttt{incransom}, \texttt{safepay}, \texttt{osiris}, \texttt{morpheus} & Threat Actors & No descriptions or TTPs provided. & Listed in the contextual CTI; no explicit connection to \texttt{officeonlaine.com}. \\
					\bottomrule
				\end{tabularx}
				
				\subsubsection*{3. Relationships -- What Is Clearly Present vs. What Remains Uncertain}
				
				\phantomsection
				\paragraph*{Clearly Present}
				
				\begin{itemize}
					\item The domain \texttt{officeonlaine.com} and the URL \url{http://officeonlaine.com/up/update.exe} are explicitly recorded as indicators with high confidence (100\%).
					\item Both indicators are tied to the source organization \textbf{iSight} and share the same external reference to the \textbf{ROBORAT} MISP event, indicating that the domain was observed in the context of ROBORAT activity.
					\item The description ``Red Kontos'' is attached to the domain indicator, but no further technical details are provided.
				\end{itemize}
				
				\phantomsection
				\paragraph*{Uncertain / Not Explicitly Linked}
				
				\begin{itemize}
					\item \textbf{Threat Actors} (\texttt{thegentlemen}, \texttt{incransom}, \texttt{safepay}, \texttt{osiris}, \texttt{morpheus}) appear in the contextual CTI but there is no explicit evidence that any of them used or operated the \texttt{officeonlaine.com} domain. Their inclusion is likely due to semantic similarity searches, not direct attribution.
					\item \textbf{Malware families} such as \textbf{PlugX}, \textbf{ZeroT}, and \textbf{LP-Notes} are listed in the contextual CTI. While they are associated with the same broader threat-intel dataset, the provided data does not show a direct relationship to the \texttt{officeonlaine.com} indicators.
					\item The \textbf{ROBORAT} malware is the only entity with a direct contextual link via the MISP event to the domain, but the nature of that link (for example whether the domain was a C2 server, dropper host, or merely observed in the same campaign) is not detailed.
				\end{itemize}
				
				\subsubsection*{4. Summary of Additional CTI Context}
				
				\begin{itemize}
					\item \textbf{Explicit IOC data:} The domain and URL are revoked, high-confidence indicators supplied by iSight, with a description ``Red Kontos''.
					\item \textbf{Contextual CTI:} The domain appears in a MISP event describing ROBORAT activity, suggesting a possible association with that malware family. Additional entities, including threat actors and other malware, are present in the same semantic-search context but lack concrete evidence tying them to \texttt{officeonlaine.com}.
				\end{itemize}
				
				\subsubsection*{5. Final Answer}
				
				The additional CTI context for \texttt{officeonlaine.com} consists of its explicit indicator details, domain and URL, and its inclusion in a MISP event that links it to the ROBORAT malware campaign. While other threat actors and malware families appear in the broader contextual dataset, no direct evidence connects them to this domain; therefore, the only concrete additional context is the ROBORAT association.
				
				\shorthandon{"} % Reactiva los shorthands de babel
			\end{tcolorbox}
		\end{small}
		
		\vspace{0.5cm}
		{\large \textbf{Valoración}}\\
		\normalsize
		\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente los dos indicadores explícitos principales: \texttt{officeonlaine.com} y la URL \url{http://officeonlaine.com/up/update.exe}, incluyendo tipo, estado, confidence, score, detection, patrón, validez, fuente y referencias externas.\\
		\textbf{¿Muestra bien el estado y confidence?} 2/2 - Recoge correctamente que ambos indicadores están \texttt{Revoked}, con \texttt{Confidence: 100}, además de los scores \texttt{60} y \texttt{30}, y \texttt{Detection: True}.\\
		\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona los threat actors inferidos \texttt{thegentlemen}, \texttt{incransom}, \texttt{safepay}, \texttt{osiris} y \texttt{morpheus}, y los malware recuperados por similitud \texttt{PlugX}, \texttt{ZeroT} y \texttt{LP-Notes}; además indica correctamente que no hay enlace explícito con el dominio y que proceden del contexto semántico.\\
		\textbf{¿Separa relaciones fuertes y débiles?} 1/2 - Separa bien la mayoría de relaciones fuertes y débiles, especialmente con los actores y malware inferidos; sin embargo, sobrefortalece ligeramente la relación con \texttt{ROBORAT}, tratándola como ``concrete additional context'' o asociación casi directa. El contexto sí contiene referencias externas a \texttt{ROBORAT}, pero no define con claridad el rol técnico exacto del dominio dentro de esa actividad.\\
		\textbf{¿Alucina?} 1/2 - No hay una alucinación grave ni atribución falsa a los actores inferidos; sin embargo, introduce inferencias algo fuertes, como hablar de ``ROBORAT malware campaign'' o sugerir que el dominio pudo estar ``used by or associated with this malware family''. Es plausible por la referencia externa, pero no está plenamente confirmado por el contexto entregado.\\
		\textbf{¿Es útil para análisis?} 2/2 - Es una respuesta muy útil para análisis CTI: estructura bien el IOC, separa indicadores explícitos de entidades inferidas, enumera actores y malware contextuales, identifica incertidumbres y responde directamente a la pregunta sobre contexto CTI adicional.\\
		\textbf{Total:} 10/12
		
		
		\subsection*{Relación con actores}
		
		\textbf{Respuesta de Lily}
		\begin{small}
			
			\begin{tcolorbox}[
				breakable,
				colback=gray!3,
				colframe=gray!50
				]
				
				Yes, based on the provided context, IP address \texttt{62.211.188.248} is explicitly associated with the threat actor \texttt{thegentlemen}. The OpenCTI intelligence indicates that this IP address has been detected as aggressive and malicious in Italy with a confidence score of 100.
				
				Additionally, some contextual CTI information suggests the existence of another threat actor named \texttt{incransom}, but no explicit association is mentioned between \texttt{62.211.188.248} and this particular actor.
				
				Looking back at the provided context, only \texttt{thegentlemen} can be attributed as the actor associated with IP address \texttt{62.211.188.248}.
				
			\end{tcolorbox}
		\end{small}
		
		\vspace{0.5cm}
		{\large \textbf{Valoración}}\\
		\normalsize
		\textbf{¿Menciona indicadores explícitos?} 1/2 - Menciona el indicador explícito 62.211.188.248, pero no recoge los atributos relevantes del indicador como Type IPv4-Addr, Status Active, Confidence 100, Score 50, Detection False, patrón STIX, fuente AbuseIPDB o descripción de IP maliciosa; al ser una pregunta centrada en atribución, esta omisión es menos grave, pero sigue siendo parcial.\\
		\textbf{¿Muestra bien el estado y confidence?} 0/2 - No menciona el estado Active ni la Confidence 100 del indicador, ni tampoco el abuseConfidenceScore 100 o el Score 50 presentes en el contexto.\\
		\textbf{¿Menciona y separa el contexto inferido?} 1/2 - Menciona uno de los actores inferidos, thegentlemen, pero ignora incransom y no explica correctamente que ambos proceden de búsqueda por similitud semántica; además trata thegentlemen como atribución cuando el contexto solo lo presenta como información inferida.\\
		\textbf{¿Separa relaciones fuertes y débiles?} 0/2 - Mezcla relación débil con relación fuerte: afirma que existe atribución de thegentlemen a la IP, cuando el contexto únicamente indica que thegentlemen e incransom son contexto inferido por similitud, no relaciones confirmadas ni atribución explícita.\\
		\textbf{¿Alucina?} 0/2 - Sí, alucina de forma relevante al afirmar una atribución confirmada a thegentlemen que no está soportada por el contexto; además omite el segundo actor inferido incransom y cambia “thegentlemen” por “thegentleman” al final.\\
		\textbf{¿Es útil para análisis?} 0/2 - No es útil para análisis porque responde incorrectamente a la pregunta principal de atribución: debería haber indicado que no hay atribución confirmada a ningún actor y que thegentlemen e incransom aparecen solo como contexto inferido no confirmado.\\
		\textbf{Total:} 2/12.\\
		
		\textbf{Respuesta de CyberOSS}
		\begin{small}
			\begin{tcolorbox} [
				breakable,
				colback=gray!3,
				colframe=gray!50
				]
				\shorthandoff{"} % Previene conflictos con las comillas dobles en inglés o formatos de fecha
				
				\subsubsection*{1. Explicit Indicators and Facts Extracted}
				
				\begin{description}
					\item[IOC Value:] \texttt{62.211.188.248} (IPv4 address)
					\item[Status:] Revoked
					\item[Confidence:] 100\%
					\item[Score:] 20
					\item[Detection:] False
					\item[Pattern:] \texttt{ipv4-addr:value = '62.211.188.248'}
					\item[Description:] Recognized as an aggressive malicious IP on AbuseIPDB, with country code \textbf{IT} (Italy). Last reported on \textbf{2026-05-09}.
					\item[Validity Period:] From \textbf{2026-05-05} to \textbf{2026-06-06}.
					\item[Source Organization:] AbuseIPDB
					\item[Marking:] TLP:CLEAR
					\item[External Reference:] AbuseIPDB database URL
				\end{description}
				
				\subsubsection*{2. CTI Entities Mentioned}
				
				\renewcommand{\arraystretch}{1.25}
				\begin{tabularx}{\textwidth}{>{\bfseries}p{3cm} p{3.5cm} X}
					\toprule
					Entity Type & Identifier & Description / Details \\
					\midrule
					Threat Actor & \texttt{thegentlemen} & No description available; created 2026-02-18. \\
					& \texttt{incransom} & No description available; created 2026-02-18. \\
					& \texttt{anubis} & No description available; created 2026-02-18. \\
					& \texttt{bravox} & No description available; created 2026-02-18. \\
					& \texttt{lapsus\$} & No description available; created 2026-03-01. \\
					\midrule
					Malware & \texttt{NanHaiShu} & Whitepaper describes it as targeting government and private-sector organizations. \\
					& \texttt{PlugX} & Detailed TTPs include Msiexec, Drive-by Compromise, Spearphishing Link, Web Protocols, DLL Search Order Hijacking, PowerShell, etc. \\
					& \texttt{LP-Notes} & No description available. \\
					\midrule
					Report / Contextual & Similarity-based & IDs such as \texttt{01ec22a8-5a4e-4a69-a1ad-297792ee17a8} link the \\
					Information & CTI entries & above threat actors and malware as contextual information. \\
					\bottomrule
				\end{tabularx}
				
				\subsubsection*{3. Relationships and Uncertainties}
				
				\phantomsection
				\paragraph*{Explicit Relationships}
				
				\begin{itemize}
					\item The IOC \texttt{62.211.188.248} is explicitly linked to AbuseIPDB as a malicious IP, with status ``Revoked'' and confidence 100\%.
					\item The contextual CTI entries list several threat actors and malware families, but none directly reference the IP address.
				\end{itemize}
				
				\phantomsection
				\paragraph*{Uncertain / Missing Relationships}
				
				\begin{itemize}
					\item No direct attribution of the IP \texttt{62.211.188.248} to any of the listed threat actors (\texttt{thegentlemen}, \texttt{incransom}, \texttt{anubis}, \texttt{bravox}, \texttt{lapsus\$}) is provided.
					\item No explicit link between the IP and the malware families (\texttt{NanHaiShu}, \texttt{PlugX}, \texttt{LP-Notes}) is provided.
					\item The similarity-based CTI provides background on actors and malware but does not confirm that they used or operated from this specific IP.
				\end{itemize}
				
				\subsubsection*{4. Final Answer}
				
				\textbf{Is the IP address \texttt{62.211.188.248} attributed to any actor according to the context?}
				
				No. The provided context identifies the IP as malicious on AbuseIPDB but does \textbf{not} associate it with any of the listed threat actors or malware. Therefore, based on the available information, the IP is not attributed to any specific actor.
				
				\shorthandon{"} % Reactiva los shorthands de babel
			\end{tcolorbox}
		\end{small}
		
		\vspace{0.5cm}
		{\large \textbf{Valoración}}\\
		\normalsize
		\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente el indicador explícito 62.211.188.248 y recoge sus atributos principales, aunque para esta pregunta el indicador explícito es secundario frente a la atribución a actores.\\
		\textbf{¿Muestra bien el estado y confidence?} 2/2 - Muestra correctamente Status: Active, Confidence 100, Score 50, Detection False, fuente AbuseIPDB, país IT, lastReportedAt, periodo de validez y referencia externa; aunque este criterio no es central para una pregunta de atribución, la respuesta lo cubre adecuadamente.\\
		\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona correctamente los threat actors inferidos thegentlemen e incransom y explica que proceden de la sección de similitud semántica, sin tratarlos como evidencia directa.\\
		\textbf{¿Separa relaciones fuertes y débiles?} 2/2 - Separa de forma muy clara la relación fuerte/confirmada entre la IP y AbuseIPDB de la relación débil/inferida con los threat actors, indicando explícitamente que no hay atribución confirmada a ningún actor.\\
		\textbf{¿Alucina?} 2/2 - No alucina atribución ni relación actor-IP; al contrario, corrige adecuadamente la posible confusión y mantiene la incertidumbre donde corresponde.\\
		\textbf{¿Es útil para análisis?} 2/2 - Es muy útil para análisis de atribución porque responde directamente a la pregunta principal, identifica que no existe atribución confirmada, menciona los actores inferidos, aclara que su relación es solo contextual y evita convertir similitud semántica en evidencia.\\
		\textbf{Total:} 12/12.\\
		
		
		\subsection*{Separación entre información confirmada e inferida}
		
		\textbf{Respuesta de Lily}
		\begin{small}
			\begin{tcolorbox}[
				breakable,
				colback=gray!3,
				colframe=gray!50
				]
				\shorthandoff{"} % Desactivación preventiva para evitar el colapso de caracteres ingleses
				
				\subsubsection*{1. Explicit Indicators and Facts Extracted from the Context}
				
				\phantomsection
				\paragraph*{Indicators explicitly associated with the IOC \texttt{officeonlaine.com}}
				
				\begin{description}
					\item[1.] The domain name \texttt{officeonlaine.com} has been revoked.
					\item[2.] The URL \url{http://officeonlaine.com/up/update.exe} associated with the IOC has also been revoked.
					\item[3.] Both indicators are marked as \texttt{Revoked} and have a confidence of 100.
					\item[4.] The validity period for both indicators is specified, indicating that they were marked as revoked on certain dates in 2025.
					\item[5.] The source organization for the indicators is listed as iSight.
					\item[6.] Both indicators have a marking of TLP:CLEAR, suggesting that they are considered open-source intelligence.
					\item[7.] The external references provided with each indicator include links to MISP events, which likely contain more contextual information about the IOC and its associated threat actors.
				\end{description}
				
				\subsubsection*{2. Information Inferred from Semantic Similarity Search}
				
				\begin{description}
					\item[1.] Two threat actor names are mentioned: \texttt{thegentlemen} and \texttt{incransom}. However, no further details or descriptions of these individuals are provided.
					\item[2.] The creation timestamps for each threat actor are stated as \texttt{2026-02-18T18:14:10.980Z} for \texttt{thegentlemen} and \texttt{2026-02-18T18:14:08.911Z} for \texttt{incransom}.
					\item[3.] The semantic similarity search results likely contain additional contextual information about the IOC, but it is not explicitly stated what that context entails.
				\end{description}
				
				\subsubsection*{Summary}
				
				In summary, OpenCTI provides concrete confirmation of the revocation status and validity period for the domain name \texttt{officeonlaine.com} and the URL \url{http://officeonlaine.com/up/update.exe} associated with it. The semantic similarity search results provide inferred information about two threat actor names but lack specific context regarding the IOC.
				
				\shorthandon{"} % Restauración segura de los shorthands en español
			\end{tcolorbox}
		\end{small}
		
		\vspace{0.5cm}
		{\large \textbf{Valoración}}\\
		\normalsize
		\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente los dos indicadores explícitos principales: officeonlaine.com y http://officeonlaine.com/up/update.exe, e identifica que ambos están relacionados con el IOC en OpenCTI.\\
		\textbf{¿Muestra bien el estado y confidence?} 1/2 - Menciona correctamente que ambos indicadores están Revoked, pero omite la Confidence 100, el Score 60 del dominio, el Score 30 de la URL y Detection: True; por tanto, cubre el estado pero no todos los atributos de confianza y puntuación.\\
		\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona los threat actors inferidos thegentlemen e incransom y aclara que proceden del contexto CTI contextual, sin tratarlos como información confirmada.\\
		\textbf{¿Separa relaciones fuertes y débiles?} 2/2 - Separa correctamente la información confirmada en OpenCTI de la información inferida por similitud, indicando que no hay confirmación explícita de relación entre los threat actors y officeonlaine.com.\\
		\textbf{¿Alucina?} 2/2 - No inventa atribuciones ni relaciones no soportadas; la respuesta se mantiene dentro del contexto y expresa correctamente la incertidumbre sobre los actores inferidos.\\
		\textbf{¿Es útil para análisis?} 2/2 - Es útil para análisis porque responde exactamente a la pregunta, distingue confirmed vs inferred, lista los indicadores confirmados, menciona la fuente iSight, TLP:CLEAR, periodos de validez, referencias externas y contextualiza correctamente que los actores inferidos no deben tratarse como atribución confirmada.\\
		\textbf{Total:} 11/12.\\
		
		\textbf{Respuesta de CyberOSS}
		\begin{small}
			\begin{tcolorbox}[
				breakable,
				colback=gray!3,
				colframe=gray!50,
				fonttitle=\bfseries
				]
				\shorthandoff{"} % Previene conflictos con las comillas dobles en inglés o formatos de fecha
				
				\subsubsection*{1. Explicit Indicators and Facts Extracted from the Context}
				
				\phantomsection
				\paragraph*{Indicators explicitly associated with the IOC \texttt{officeonlaine.com}}
				
				\renewcommand{\arraystretch}{1.25}
				\begin{tabularx}{\textwidth}{>{\bfseries}p{3cm} p{2.2cm} p{2cm} p{1.8cm} X}
					\toprule
					\textbf{Indicator} & \textbf{Type} & \textbf{Status} & \textbf{Confidence} & \textbf{Details} \\
					\midrule
					\texttt{officeonlaine.com} 
					& Domain-Name 
					& Revoked 
					& 100 
					& Score: 60; Detection: True; Pattern: \texttt{[domain-name:value = 'officeonlaine.com']}; Description: Red Kontos; Validity: 2025-01-16T17:11:19.000Z to 2025-11-12T17:11:19.000Z; Source: iSight; Marking: TLP:CLEAR; External references: MISP event ``ROBORAT Malware Targets Think Tanks...'' and MISP event ``The Lowdown...''. \\
					\hline
					\texttt{http://officeonlaine.com/up/update.exe} 
					& URL 
					& Revoked 
					& 100 
					& Score: 30; Detection: True; Pattern: \texttt{[url:value = 'http://officeonlaine.com/up/update.exe']}; Description: Red Kontos; Validity: 2025-01-16T16:54:49.000Z to 2025-03-04T16:54:49.000Z; Source: iSight; Marking: TLP:CLEAR; External reference: MISP event ``ROBORAT Malware Targets Think Tanks...''. \\
					\bottomrule
				\end{tabularx}
				
				\phantomsection
				\paragraph*{Key explicit facts}
				
				\begin{itemize}
					\item The domain \texttt{officeonlaine.com} is listed as a \textbf{revoked} domain-name indicator.
					\item A specific URL, \url{http://officeonlaine.com/up/update.exe}, is listed as a \textbf{revoked} URL indicator.
					\item Both indicators have \textbf{100\% confidence} and are marked as \textbf{detected}.
					\item They are attributed to the \textbf{Red Kontos} campaign.
					\item The indicators are valid from \textbf{16 January 2025} until \textbf{12 November 2025} for the domain and \textbf{4 March 2025} for the URL.
					\item The source organization is \textbf{iSight} and the marking is \textbf{TLP:CLEAR}.
					\item External references point to two MISP events: the ROBORAT malware campaign and ``The Lowdown'' from April 2018.
				\end{itemize}
				
				\subsubsection*{2. CTI Entities Mentioned in the Context}
				
				\renewcommand{\arraystretch}{1.25}
				\begin{tabularx}{\textwidth}{>{\bfseries}p{3cm} p{2.7cm} X X}
					\toprule
					\textbf{Entity} & \textbf{Type} & \textbf{Description / Role} & \textbf{Relationship to \texttt{officeonlaine.com}} \\
					\midrule
					Red Kontos 
					& Campaign / Actor 
					& The campaign that uses the domain and URL. 
					& Directly linked to both indicators, domain and URL. \\
					\hline
					ROBORAT 
					& Malware 
					& Targets think-tanks, human-rights groups, diplomacy and hospitality; suspected Kazakhstan nexus. 
					& Cited in the external reference for the domain indicator. \\
					\hline
					The Lowdown 
					& Report / Event 
					& A 2018 MISP event describing cyber operations. 
					& Cited in the external reference for the domain indicator. \\
					\hline
					iSight 
					& Organization 
					& Provides the indicator data. 
					& Source of both indicators. \\
					\hline
					TLP:CLEAR 
					& Marking 
					& Traffic Light Protocol marking indicating public sharing. 
					& Applied to both indicators. \\
					\hline
					MISP 
					& Platform 
					& The platform where the external references are hosted. 
					& Hosts the two cited events. \\
					\hline
					Threat actors 
					& Inferred Actors 
					& \texttt{thegentlemen}, \texttt{incransom}, \texttt{safepay}, \texttt{osiris}, \texttt{morpheus}; no descriptions or TTPs provided. 
					& Listed under contextual CTI information inferred from similarity; no explicit link. \\
					\hline
					Malware families 
					& Inferred Malware 
					& \texttt{PlugX}, \texttt{ZeroT}, \texttt{LP-Notes}. 
					& No explicit link to \texttt{officeonlaine.com}. \\
					\bottomrule
				\end{tabularx}
				
				\subsubsection*{3. Confirmed vs. Inferred Information about \texttt{officeonlaine.com}}
				
				\renewcommand{\arraystretch}{1.25}
				\begin{tabularx}{\textwidth}{>{\bfseries}p{4cm} X X}
					\toprule
					\textbf{Aspect} & \textbf{Confirmed in OpenCTI} & \textbf{Inferred / Uncertain} \\
					\midrule
					Domain name 
					& Yes. Listed as a revoked domain indicator with full metadata. 
					& -- \\
					\hline
					Associated URL 
					& Yes. Listed as a revoked URL indicator with full metadata. 
					& -- \\
					\hline
					Campaign / Threat-actor 
					& Yes. Both indicators are attributed to the \textbf{Red Kontos} campaign. 
					& -- \\
					\hline
					Malware used 
					& Yes. The domain is linked to \textbf{ROBORAT} malware via external reference. 
					& No direct evidence that \texttt{officeonlaine.com} hosts or delivers the malware; the link is indirect through the external reference. \\
					\hline
					Target sectors 
					& Yes. The external reference describes the sectors targeted by the associated malware: think-tanks, human-rights, diplomacy and hospitality. 
					& No direct evidence that \texttt{officeonlaine.com} itself targets those sectors. \\
					\hline
					Geopolitical nexus 
					& Yes. The external reference mentions a suspected \textbf{Kazakhstan nexus} for the campaign. 
					& No direct evidence tying the domain itself to Kazakhstan. \\
					\hline
					Temporal validity 
					& Yes. Explicit ``valid from'' and ``valid until'' timestamps are provided for both indicators. 
					& -- \\
					\hline
					Source organization 
					& Yes. \textbf{iSight} is the source of the indicators. 
					& -- \\
					\hline
					Marking 
					& Yes. \textbf{TLP:CLEAR} is applied. 
					& -- \\
					\hline
					Other threat actors / malware 
					& No. Entities such as Thegentlemen, PlugX and ZeroT are listed only in the inferred section and are not linked to the domain. 
					& Inferred only; no direct relationship. \\
					\bottomrule
				\end{tabularx}
				
				\subsubsection*{4. Summary of Relationships}
				
				\phantomsection
				\paragraph*{Confirmed relationships}
				
				\begin{itemize}
					\item The domain and URL are directly tied to the \textbf{Red Kontos} campaign, with full indicator metadata such as status, confidence, validity, source and marking.
					\item The external references connect the campaign to \textbf{ROBORAT} malware and the ``The Lowdown'' report, establishing an indirect but documented link to the malware's targeting profile and geopolitical context.
				\end{itemize}
				
				\phantomsection
				\paragraph*{Uncertain / inferred relationships}
				
				\begin{itemize}
					\item No explicit evidence shows that \texttt{officeonlaine.com} itself hosts or distributes the \textbf{ROBORAT} malware.
					\item No explicit evidence shows that \texttt{officeonlaine.com} directly targets the listed sectors.
					\item The other threat actors and malware, such as Thegentlemen, PlugX and ZeroT, appear only in the similarity-based inferred section and have no explicit connection to the domain.
				\end{itemize}
				
				\subsubsection*{5. Final Answer}
				
				\phantomsection
				\paragraph*{Confirmed in OpenCTI}
				
				\begin{itemize}
					\item The domain \texttt{officeonlaine.com} and the URL \url{http://officeonlaine.com/up/update.exe} are revoked indicators with 100\% confidence.
					\item Both indicators are attributed to the \textbf{Red Kontos} campaign.
					\item The domain indicator is valid from 16 January 2025 to 12 November 2025, while the URL indicator is valid until 4 March 2025.
					\item The source organization is \textbf{iSight}, and the indicators are marked \textbf{TLP:CLEAR}.
					\item External references link the campaign to \textbf{ROBORAT} malware and the ``The Lowdown'' report, indicating targeting of think-tanks, human-rights groups, diplomacy and hospitality sectors, with a suspected Kazakhstan nexus.
				\end{itemize}
				
				\phantomsection
				\paragraph*{Inferred / Uncertain}
				
				\begin{itemize}
					\item There is no direct evidence that \texttt{officeonlaine.com} itself hosts or delivers the \textbf{ROBORAT} malware; the link is indirect via the external reference.
					\item The domain's role in targeting the specific sectors or its geopolitical nexus is inferred from the external references, not directly observed.
					\item Other threat actors and malware listed in the similarity-based section, such as Thegentlemen, PlugX and ZeroT, have no explicit relationship to \texttt{officeonlaine.com}.
				\end{itemize}
				
			\end{tcolorbox}
		\end{small}
		
		\vspace{0.5cm}
		{\large \textbf{Valoración}}\\
		\normalsize
		\textbf{¿Menciona indicadores explícitos?} 2/2 - Menciona correctamente los dos indicadores explícitos principales, officeonlaine.com y http://officeonlaine.com/up/update.exe, incluyendo sus patrones, estados, confidence, score, detection, periodos de validez, fuente, marking y referencias externas.\\
		\textbf{¿Muestra bien el estado y confidence?} 2/2 - Muestra correctamente que ambos indicadores están Revoked y con Confidence 100; también recoge Score 60 para el dominio, Score 30 para la URL y Detection True en ambos casos.\\
		\textbf{¿Menciona y separa el contexto inferido?} 2/2 - Menciona correctamente los threat actors inferidos thegentlemen e incransom, indica que proceden de similarity search y aclara que no hay evidencia directa que los vincule al dominio o a la URL.\\
		\textbf{¿Separa relaciones fuertes y débiles?} 1/2 - Separa bien la relación débil de los threat actors inferidos, pero sobrefortalece algunas relaciones derivadas de referencias externas: trata ROBORAT/campaign e “infraestructura usada por la campaña” como algo prácticamente confirmado, cuando el contexto solo muestra referencias externas relacionadas y no una relación analítica explícita completa sobre el rol operacional del dominio y la URL.\\
		\textbf{¿Alucina?} 1/2 - No inventa una atribución a thegentlemen o incransom, pero sí introduce inferencias algo fuertes al afirmar que la campaña está identificada como ROBORAT, que el dominio y la URL pertenecen a una única campaña y que update.exe sugiere un payload malicioso; esas inferencias son plausibles por las referencias, pero no están plenamente confirmadas por el contexto proporcionado.\\
		\textbf{¿Es útil para análisis?} 2/2 - Es útil para análisis porque responde directamente a la pregunta, estructura confirmed vs inferred, enumera los indicadores explícitos, menciona los actores inferidos y marca incertidumbres; aun con alguna sobreinterpretación, ofrece una visión analítica rica y accionable.\\
		\textbf{Total:} 10/12.




\newpage
\addcontentsline{toc}{section}{Bibliografía}
\printbibliography[title={Bibliografía}]


\end{document}